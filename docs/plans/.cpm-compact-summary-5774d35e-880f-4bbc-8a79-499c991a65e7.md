# Compact Summary (PostCompact)
**Captured**: 2026-09-17 15:54:30
**Trigger**: auto

<analysis>
The session continued from an earlier compaction. It went: nativ drafter setup, abandoning nativ, returning to MTPLX with the speed model, then rebuilding the spec → epics → do chain under ~/dpm-runs/tally-speed. The dpm-do stage is running now.
</analysis>

<summary>
1. **Primary Request and Intent**

   **Earlier in the session (before this segment):**
   - Chris switched from MTPLX to nativ.
   - I extracted an MTP drafter and set up a harness.

   **This segment:**
   - Chris couldn't tell whether the drafter was selected. I found that nativ pairs drafters to models by name and renamed the drafter accordingly. It worked: the Speed menu showed "Fast".
   - Chris approved throughput tests:
     - with the drafter: 12.2 tok/s at 100% acceptance, against 7.8 tok/s without;
     - reasoning off via a top-level `enable_thinking`.
   - Chris: "I don't think we're going to get anywhere with nativ. Mtplx seems better. Can we go back to pi with mtplx, but use the speed model we have (4 bit)".
     - I quit nativ and started MTPLX with the speed model.
     - I deleted the drafter copies.
     - My attempt to delete the nativ HF cache copy of the model (28 GB) was blocked by the classifier. I told Chris to run `! rm -rf ~/.cache/huggingface/hub/models--Youssofal--Qwen3.8-27B-MTPLX-Optimized-Quality-FP16` themselves.
   - Chris: "Okay then. Let's continue". The project had been lost, so I asked; Chris chose "Rebuild the chain" (spec → epics → do).
   - Chris: "Don't build in scratchpad". Everything now lives in `~/dpm-runs/tally-speed/`.
   - Chris: "You need to keep me better informed". Short updates on every monitor event and a 5-minute heartbeat are now required.
   - Cache-cap question: Chris first chose "Stop for now". Later, when the context grew past ~60k tokens, Chris chose "Let spec finish, raise after".
   - Chris: "Last time we raised to 16GB we ran out of memory, albeit with the other model". I proposed 10 GB and Chris said "Yes".
   - Chris: "Fans are still running". The cause was MTPLX's smart fan mode leaking its active-request count.
     - Chris: "Restart with fan mode default. Then stop. This should clear the fans".
     - Done; the fans cleared.
   - pytest for dpm-do: Chris chose "Pre-create a venv (Recommended)".

   **Standing constraints (carried forward):**
   - Use the Edit tool for existing files; no sed/perl/awk edits.
   - Commit or push only when asked. Tags stay local.
   - Don't edit `~/.pi/agent/models.json` without asking.
   - Don't restart or change the MTPLX server without asking. Chris approved: speed model, `--fan-mode default`, 10 GB cap.
   - Ask before launching runs. Chris approved the whole chain.
   - Don't change nativ settings without asking (nativ is now abandoned).
   - Address Chris by name; use they/them.
   - Monitor long runs actively. Relay every gate, error and heartbeat as it arrives. Re-arm the monitor on every 30-minute expiry. Never let the monitor go silent.
   - Don't build in the scratchpad.
   - Commits end with: `Claude-Session: https://claude.ai/code/session_01SvAKbN8z9Zdfh9gorEkL6T`

2. **Key Technical Concepts**

   **nativ findings (abandoned, kept in memory):**
   - `DrafterModelCompatibility.isCompatible` matches names via `pairingKey`: tags such as mtp, drafter, fp16 and Nbit are stripped, and the remaining name must match exactly. Hidden size must also match.
   - The server reads only a top-level `enable_thinking`. It ignores `chat_template_kwargs`.
   - Per-model defaults come from the `NATIV_MODEL_CONFIGS` env var, read at server start.
   - pi's `thinkingFormat: "qwen"` sends a top-level `enable_thinking`.

   **MTPLX v2.11.2:**
   - Session-bank cap env vars: `MTPLX_SESSION_BANK_MAX_BYTES` and `MTPLX_SESSION_BANK_PER_SESSION_BYTES`.
   - KV cache costs about 88 KB per token. A 59k-token context needs 5.22 GB, over a 5 GB cap. That produced full re-reads (`cache_source none`, `ssd_prefix_miss`) taking 13–23 minutes past ~60k tokens.
   - At a 10 GB cap, the cache held to 84k tokens.
   - `--fan-mode smart` leaked `smart_fan_active_count` (29) when requests were killed. This held `fan_boost_active` true while idle. `mtplx stop --port 8000` releases the fans.
   - Request log: `~/.mtplx/logs/request-log-8000.jsonl`, with fields prompt_tokens, cached_tokens, ttft_s, cache_miss_reason.
   - `/health` reports active_requests and fan_boost_active.

   **Cap history (quality build):**
   - no cap: peak 44.85 GB
   - 8 GB cap: peak 52.7 GB
   - 16 GB: OOM
   - hence 5 GB then; now 10 GB for the speed build (19.3 GB weights)

   **Slower prefill:**
   - Prefill slowed from ~90 to ~50–60 tok/s.
   - No thermal warning was recorded.
   - Likely CPU/GPU contention: Chrome ~400% CPU, Arq backup 86%, Screen Sharing 47%.

   **dpm skills:**
   - **dpm-spec** Step 6b: criteria only for must-have functional requirements plus all non-functional requirements. Environment entries get their own gate.
   - **dpm-epics:**
     - hands off to a fresh context per epic, plus one before confirm;
     - adopts sessions cleanly;
     - per epic, works through stories → criteria → tags → tasks → coverage, then confirm.
   - A document's `status` of `pending` for ADRs is a lifecycle status. The decision lives in `adr.decision_status`, which is `accepted`.
   - **dpm-do:** offers only ready epics (01-02 is blocked by its dependency on 01-01).

   **Monitor pitfalls:**
   - Every pipe stage must flush line by line; a trailing `| cut` buffered everything.
   - Use `tail -F`.
   - Include a heartbeat.
   - Exit when the driver stops.

3. **Files and Code Sections**

   **`~/dpm-runs/tally-speed/drive.mjs`**
   - Copied from `scratchpad/nativ-do`.
   - Header comment edited to describe tally-speed.
   - Defaults changed:
     ```js
     const PROVIDER = env.DRIVE_PROVIDER ?? 'mtplx';
     const MODEL = env.DRIVE_MODEL ?? 'mtplx-qwen38-27b-optimized-speed-fp16';
     ```
   - Args are `project home out PROMPT`.
   - Writes `gates.md`, `events.jsonl` and `status.json` (gates, errors, blockedGates, modelErrors, handoffs, lastTool).
   - Follows handoffs.

   **`~/dpm-runs/tally-speed/home/models.json`**
   - Copy of `~/.pi/agent/models.json`.
   - Providers: mtplx (speed model, `thinkingFormat` qwen, `maxTokens` 32768) and nativ.

   **`~/dpm-runs/tally-speed/` stage folders:**
   - `1-spec/`: first spec run. Stopped at 3807 s, 14 gates.
   - `1-spec-resume/`: resume attempt. It adopted the session but restarted criteria at FR1; a UNIQUE constraint was hit; a near-duplicate must_not went in; stopped.
   - `1-spec-b/`: fresh spec, complete.
     - 3 h 26 min, 18 gates, 1 refused gate, peak swap 71 MB.
     - Contents: 12 FR, 2 NFR, 8 ENV (5 requirements, 3 restrictions), 3 accepted ADRs (Python 3.11 stdlib only, run-from-checkout, parse→aggregate→render pipeline), 16 criteria.
   - `2-epics/`: `/dpm-epics 01`, complete.
     - 3 h 44 min, 28 gates, 4 no-text refusals, 3 handoffs.
     - Contents: 3 epics (01-01 pipeline, 01-02 command, 01-03 verification/test suite), 11 stories, 25 tasks, 34 coverage links; no gaps; 15 integrity invariants hold.
   - `3-do/project`: copy of the epics project.
     - `git init` and commit "Spec and breakdown" (fb488e0), author tally.
     - `.venv` created with `/opt/homebrew/bin/python3.11 -m venv`; pytest 9.1.1 installed.
     - `.venv/` added to `.git/info/exclude`.

   **MTPLX server (currently running, background task boiqjn7bj):**
   ```
   MTPLX_SESSION_BANK_MAX_BYTES=10G MTPLX_SESSION_BANK_PER_SESSION_BYTES=10G mtplx serve --port 8000 --no-auth --fan-mode default --enable-thermal-poll --preserve-thinking auto --tool-prompt-mode native --model mtplx-qwen38-27b-optimized-speed-fp16
   ```

   **dpm-do launch (background task b3fbpvdbb):**
   ```
   B=/Users/chris/dpm-runs/tally-speed && P=$B/3-do/project && cd $P && VIRTUAL_ENV=$P/.venv PATH=$P/.venv/bin:$PATH node $B/drive.mjs $P $B/home $B/3-do/out '/dpm-do' > $B/3-do/driver.log 2>&1
   ```

   **Current monitor (task bkjad4zbh, 30-minute expiry). Re-arm with the same command:**
   ```
   B=/Users/chris/dpm-runs/tally-speed; S=$B/3-do/out; F=$S/gates.md
   until [ -f $F ]; do sleep 1; done
   tail -n +1 -F $F | grep --line-buffered -E '^## Gate|^\*\*[A-Z]|phase "|TOOL_ERROR|MODEL_ERROR|extension_error|refused|notice|finished:|pi exited|^## Handoff|bash: .*(pytest|git commit)' &
   tail -n 0 -F ~/.mtplx/logs/request-log-8000.jsonl | python3 -u -c "
   import sys,json
   for l in sys.stdin:
       try: d=json.loads(l)
       except: continue
       p=d.get('prompt_tokens') or 0; c=d.get('cached_tokens') or 0
       if p>4000 and (p-c)>10000:
           print('SLOW PREFILL: prompt',p,'cached',c,'uncached',p-c,'ttft',round(d.get('ttft_s') or 0),'s', flush=True)
   " &
   while sleep 300; do
     st=$(cat $S/status.json 2>/dev/null); sw=$(sysctl -n vm.swapusage | awk '{print $6}')
     pgrep -f "[n]ode $B/drive.mjs" >/dev/null && r=running || r=STOPPED
     echo "heartbeat: $r $st swap=$sw"
     [ $r = STOPPED ] && exit 0
   done
   ```
   When re-arming after the run has started, use `tail -n 0` instead of `tail -n +1`, and drop the `until` line.

   **Memory files (updated):**
   - `memory/nativ-backend.md`: new file; marked **Abandoned 2026-09-16**; drafter copies deleted.
   - `memory/dpm-do-run-followups.md`:
     - the speed-model start command with `--fan-mode default` and the fan-leak bug;
     - the tally-speed chain location;
     - spec stage 1 findings: the invented ADR option id, and the resume restarting criteria at FR1 plus a raw UNIQUE error;
     - cap history and the approved 10 GB cap;
     - spec-b and epics results and findings;
     - the 3-do venv setup.
   - `memory/monitor-long-runs.md`: added the flush pitfall, the mandatory heartbeat, and Chris's quote.
   - `MEMORY.md`: added the nativ-backend line.

   **nativ source clone (read only):** `scratchpad/nativ-src`, used for `ChatComposer.swift` and `LocalModelDiscovery.swift`.

4. **Errors and fixes**
   - **Drafter not offered in nativ:** caused by the name mismatch (missing "Optimized"). Fixed by renaming the HF cache folder.
   - **`chat_template_kwargs` ignored by nativ:** a top-level `enable_thinking` works. Added `thinkingFormat: "qwen"` to the harness.
   - **Classifier blocked the rm of the 28 GB model:** Chris was told to run it themselves.
   - **Monitor silent for 25 minutes:** a trailing `cut` buffered output. Rebuilt without it, with a heartbeat. Chris's feedback: "You need to keep me better informed".
   - **I wrongly predicted every turn would miss the cache after the first 59k miss:**
     - The first run recovered, and I corrected myself.
     - In spec-b, misses did persist past ~60k with the 5 GB cap.
   - **I suggested thermal throttling:** pmset showed no warning, and I corrected that. Contention from Chrome and Arq is the likely cause.
   - **Resume attempt went wrong:** it redid criteria from FR1. Stopped, and a fresh spec was started.
   - **Suggested a 16 GB cap:** Chris pointed out the earlier OOM. Changed to 10 GB.
   - **Fans stuck on:** stopped and restarted MTPLX with `--fan-mode default`.
   - **pytest missing:** venv pre-created, as Chris chose.
   - **Minor:** zsh glob errors (use quotes), an ambiguous SQL column, a blocked `sleep` chain (use Monitor or background tasks instead).

5. **Problem Solving**
   - The chain rebuild is under way; spec and epics are done.
   - **Findings recorded for pi-dpm:**
     - model batched ADR tradeoffs citing an invented option id;
     - spec session state lacks per-FR criteria progress, so a resume restarts criteria;
     - `create_acceptance_criterion` returns raw UNIQUE text on duplicates;
     - repeated no-text gate refusals, recovered each time;
     - epics verification context created criterion approaches before their tag gate;
     - raw `{{ref:<id>}}` leaked into gate text;
     - CI criterion tagged `unit`.
   - **Open question:** prefill slowdown from contention; Chris was told closing Chrome or pausing Arq may help.

6. **All user messages**
   - "there's this [Image #1] and i've found the drafter [Image #2] but i have no idea how to confirm it's been selected (or even how to do this) or if it's connected to the model in chat"
   - "yes, and it's loaded the model"
   - "yes, run it"
   - "i've turned reasoning off"
   - "yes, do both"
   - "I don't think we're going to get anywhere with nativ. Mtplx seems better. Can we go back to pi with mtplx, but use the speed model we have (4 bit)"
   - "Yes" (delete the drafter copies)
   - "Yes" (delete the nativ HF model copy; blocked)
   - "Okay then. Let's continue"
   - AskUserQuestion answer: "Rebuild the chain (Recommended)"
   - "Don't build in scratchpad" (mid-turn)
   - "What's the progress"
   - "You need to keep me better informed"
   - AskUserQuestion answer: "Stop for now" (cache cap during the first spec)
   - "Fans are still running"
   - "Restart with fan mode default. Then stop. This should clear the fans"
   - "Okay let's continue"
   - AskUserQuestion answer: "Let spec finish, raise after (Recommended)"
   - "Last time we raised to 16GB we ran out of memory, albeit with the other model"
   - "Yes" (10 GB cap)
   - AskUserQuestion answer: "Pre-create a venv (Recommended)"

7. **Pending Tasks**
   - Monitor the dpm-do run (stage 3) to completion:
     - relay every gate, phase, error, slow prefill and heartbeat to Chris;
     - re-arm the monitor at each 30-minute expiry.
   - When it finishes, summarise results and findings, and update `memory/dpm-do-run-followups.md`.
   - Watch swap with the 10 GB cap.

8. **Current Work**
   dpm-do (stage 3) is running in `~/dpm-runs/tally-speed/3-do/project`.

   **Events so far:**
   - First turn read ~21k tokens from scratch (244 s).
   - Session created at 342 s, phase select.
   - Gate 1 at 377 s chose epic 01-01 Pipeline (Recommended); only 01-01 and 01-03 were offered, since 01-02 depends on 01-01.
   - Harmless bash TOOL_ERROR at 432 s: the model probed for composer.json, package.json and a Makefile.
   - Phase "load" at 574 s.
   - Latest heartbeat: `{"at":"600s","gates":1,"errors":1,...,"handoffs":0,"lastTool":"dpm_list_document_section"} swap=70.56M`

   The last assistant message reported the load phase: "**10 min:** story selected, and the run is at **load**, reading the story's criteria and tasks."

9. **Optional Next Step**
   Keep relaying monitor events for the dpm-do run. Each update is a short status line: time, event, gates/errors, swap. Re-arm monitor `bkjad4zbh` (same command, `tail -n 0`) when it expires. Per Chris: "You need to keep me better informed". Report the run's outcome when the driver exits.
</summary>
