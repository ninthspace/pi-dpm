# Compact Summary (PostCompact)
**Captured**: 2026-09-16 08:08:01
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Segment 1 — continuation from a compacted session.** The summary at the top establishes: repo `/Users/chris/Work/git/pi-dpm` ports opencode-dpm to pi; Chris tests dpm skills against a local MTPLX server on port 8000 via a scratchpad RPC driver that auto-answers gates. Standing constraints were listed (Edit tool only, no sed/perl/awk for edits; commit/push only when asked; don't point load generators at :8000; don't edit `~/.pi/agent/models.json` without asking; don't restart/change the MTPLX server or config without asking; posting to MTPLX #277 needs go-ahead; pi-dpm may diverge from opencode-dpm; address the user as Chris, they/them; monitor long runs actively; ask before launching runs; don't edit extension files while a run is live).

The pending task was making dpm-do run exactly one epic. I resumed and made four edits:
- `skills/dpm-do/SKILL.md` Input: "The epic, once resolved, holds for the whole run. **One run works one epic** and ends at Step 8..."
- Step 8 ending: "**Then the run ends.** `dpm_update_session` with `phase: 'complete'`... Name the next ready epic from `dpm_list_epic` with `ready: true`... **This run does not work it, and does not ask whether to**..."
- Guidelines: replaced "the epic-end offer" with "a retired story at Step 8" and added "It ends at Step 8, after one epic, naming the next rather than asking to start it."
- `tests/skill-do.test.js`: new test 'a run works one epic, closes its session, and names the next epic without working it'.

Full suite: 1282 tests, 0 failures.

**Segment 2 — "Let's try this."** Chris approved a live run. I inspected the epics7 scratchpad (`drive.mjs`, `monitor.sh`, `out/`, `project/.dpm/dpm.db`). Key discovery: driver used `--no-builtin-tools`, which dpm-do can't use (needs read/bash/edit/write). Also Go not installed while spec ADR 01-01 pins Go.

I asked two questions via AskUserQuestion; Chris answered "Let the run install it" (Go) and "Let dpm-do pick" (epic).

Setup: copied epics7 project/home/drive.mjs/monitor.sh to `scratchpad/mtplx-do1`, git init + commit in project. Three edits to the copied `drive.mjs`: header comment, removed `--no-builtin-tools`, added bash-command logging (`tool_execution_start` + `record.toolName === 'bash'`).

First launch attempt was **blocked by the Claude Code auto-mode classifier**. I stopped and explained; Chris said "You can add the bash permission rule". I created `/Users/chris/Work/git/pi-dpm/.claude/settings.local.json` with one allow rule for `Bash(node /private/tmp/.../mtplx-do1/drive.mjs:*)`, then launched successfully in background + armed Monitor on `monitor.sh out`.

**Segment 3 — monitoring part 1 (out/).** Timeline captured: 232s shared doc read + list_epic; 341s question blocked by UNRENDERED guard (no text); Gate 1 at 362s picking epic 01-01 CLI contract; session created; discovery (`ls` glob exit 1 = error 2); load; `state` recorded epic/test_command `go test ./...`/framework; 583s `go version` failed, 599s probe failed (error 3); Gate 2 at 653s = Go blocker gate with three options; 676s model ran `brew install go` itself → Go 1.27.1 installed at `/opt/homebrew/bin/go`; task 1 written (go.mod, main.go, cli.go), built, vetted; task 1 complete at 1175s; task 2 help.go; task 3 tests cli_test.go/main_test.go; `go test ./...` green at 1518s; coverage verified (3 rows, model-typed `verified_at: "2026-09-15T19:17:00Z"`); story 1 complete at 1715s + observation + category; Handoff 1 at 1795s (phase select at 1785s); fresh context adopted session 01a0a684 with predecessor 01a0a669.

Chris said "Make a note for the model to write verified_at" then corrected mid-turn "Sorry tool" — meaning the *tool* should write verified_at. I created memory `/Users/chris/.claude/projects/-Users-chris-Work-git-pi-dpm/memory/dpm-do-run-followups.md` and added an index line to MEMORY.md.

Story 2 work: ingest.go (checkExpenses), 2 edit mismatches (errors 4, 5), gofmt/vet/build/exit-code probes writing scratch files to /tmp, task 1 complete 3036s, task 2 (panic contract, TALLY_TEST_PANIC) complete 3319s, task 3 exit_test.go, verify listing coverage at 3785s, 10 update_coverage at 3873s, refactor bash at 3922s.

**Segment 4 — the kill.** Claude Code's low-memory handling killed both background tasks: the MTPLX server (bzoe3rmj3) and the driver (bqzngcwrj). I checked: server down, no orphan processes, story 2 tasks 1–3 complete but story pending, session phase `verify` with `story_in_flight` note. I stopped the monitor (TaskStop b0psu112p) and updated the memory file with the phase-label finding and the kill.

Chris challenged: "I also don't believe that Claude code kills other apps..." I verified with `log show` — no jetsam entries — and corrected myself: Claude Code only killed its own background tasks, which were mine because I ran the server as a background task.

**Segment 5 — fans.** Chris: "Something is still running. Can you check the fans?" I checked ps/GPU/thermal (all idle), suggested powermetrics; Chris reported "powermetrics: unrecognized sampler: smc"; I checked the sampler list (no SMC on Apple Silicon) and suggested thermal/cpu_power/gpu_power; Chris pasted readings (0.66W, Nominal, GPU 38% at 389MHz). Chris: "fans are still running". I checked model (M1 Max MBP), battery, disk, thermal logs — all clean. Suggested Stats. Chris: "i've install stats". I found `/Applications/Stats.app/Contents/Resources/smc`, ran `smc fans` → **both fans `forced` at max (5776/5779, 6201/6241)**, temps 22–50°C. I searched the transcript and found the MTPLX serve command includes `--fan-mode smart --enable-thermal-poll`. I gave `sudo .../smc reset`. Chris: "done, fans are quiet now" — confirmed `automatic`. Chris also observed: "I think the fans were left such because mtplx was killed rather than shutdown properly" — correct.

**Segment 6 — resume.** Chris: "Okay let's go again". I recovered the serve command from the transcript, asked about fan policy (Chris: "Keep --fan-mode smart") and session bank (Chris: "Restart with 5G bank"). Started server, stopped it (TaskStop — fans stayed automatic, proving clean stop works), restarted with `MTPLX_SESSION_BANK_MAX_BYTES=5G MTPLX_SESSION_BANK_PER_SESSION_BYTES=5G`, confirmed "session-bank budget: 5.0G total (explicit)". Launched resume run writing to `out2` with prompt `/dpm-do`, armed Monitor.

**Segment 7 — monitoring part 2 (out2/).** 210s shared doc; 278s list_session/list_epic/list_retro/list_library (parallel, before seeing session result); 345s `dpm_adopt_session` id 01a0a8e3 predecessor 01a0a684 — state carried `story_in_flight` intact; re-ran `go test ./...` (ok); read 6 distinct files; no duplicate coverage stamping (0 update_coverage in out2); refactor at 681s; story 2 complete at 1022s with observation; Handoff 2 at 1100s (phase next 1049s → select 1093s); fresh context 1316s adopt + shared doc; 1391/1401s list_story (ready:true then unfiltered) — Step 8 path; 1417s `dpm_update_epic` → cli-contract complete; 1446s list_requirement/list_story_criterion/list_observation(include_body, unscoped); 1515s list_coverage; 1713s list_taxonomy + list_epic(ready:true) returning the other three epics; 1760s `date -u` → 2026-09-16T06:53:32Z; 1777s two `dpm_update_requirement` with `coverage_claimed_at` (FR4, NFR2 — both wholly bound in cli-contract, all verified); 1842s `phase: 'complete'`; 1877s final summary text and `pi exited 0`.

The summary text named `01-02` "CSV ingest" and said "this run is finished" — the single-epic behaviour worked. But it claimed "12 of the 12 bindings" and "6 under FR3" when the DB says 13 (story 1: FR4/FR5/FR7 = 3; story 2: FR3 = 7, NFR2 = 3), all verified. I recorded that as a fourth finding in memory.

**Segment 8 — Chris asked "How do we actually clear context in our dpm toolset?"** I read `extensions/dpm/handoff.ts` and explained: tool marks + command carries out; `terminate: true`; ALREADY_HANDED_OFF block; `agent_settled` → `sendUserMessage('/dpm-continue <skill> <args>')`; command-only `newSession`; hidden continuation note naming predecessor; adopt_session; continuity in rows not prose. Chris observed Claude Code can only clear context manually; I compared auto-compaction (lossy) vs typed rows, and noted subagents as the Claude Code analogue.

**Segment 9 — "Do the follow ups".** Four findings to build:
1. verified_at stamped by tool not model
2. phase labels skipped
3. /tmp scratch files
4. Step 8 roll-up miscount

Research findings:
- `src/tools/spine/coverage.ts`: docblock lines 12–17 deliberately says "`verified_at` is the caller's, `binding_hash` is computed"; `STATE` at 131–137 has position + verified_at; `coverageTools({ db, now, newId }: Context)`; create handler line 162–178; update handler 218–228; `retire_coverage` with `serverSupplied: { retired_at: SUPPLIED.clock }` and `now()` — the precedent.
- `SUPPLIED` in convention.ts line 134 (`ulid`, `clock`, `allocated`, `derived`); `DEFAULT_LIMIT = 50` (so paging didn't cause the miscount — arithmetic did).
- `spineTools(db, { now = () => new Date().toISOString(), ... })`.
- Ten test suites pass verified_at through tools; `tools.test.js` pins `now: () => STAMP` (STAMP = '2026-08-08T12:00:00.000Z'); `criterion-supersession`, `supersession-retirement`, `skill-do`, `skills-corpus`, `substrate-amendments`, `skill-inspect`, `skill-status` use unpinned `spineTools(db)`.
- Exact-value assertions: tools.test.js:395, skill-do.test.js:330, skills-corpus.test.js:319, supersession-retirement.test.js:219, substrate-amendments.test.js:398.

Edits made (follow-ups 2, 3, 4):
- `src/tools/cross/coverage-check.ts`: added `coverage.verified_at` to SELECT; signature `coverageReport(db, specId, epicId = null)`; `epicScope` IIFE block computing per-epic bindings/verified/by_requirement with two ToolError refusals; `epic: epicScope` in the return; description addition; inputSchema `epic_id` + `handler: (args) => coverageReport(db, args.spec_id, args.epic_id ?? null)`; docblock paragraph about the 12-vs-13 miscount.
- `tests/coverage-check-tool.test.js`: two new tests (epic count without moving standings; refusals).
- `skills/dpm-do/SKILL.md`: Step 8 count wording now mandates `dpm_check_coverage` with epic_id; Step 6 Session phase-label discipline; Step 4 scratch-files paragraph.

Test run: `tests/coverage-check-tool.test.js tests/skill-do.test.js` → one failure: 'the skill recovers nothing by reading a generated file' — my Step 6 wording "from the list this skill's front matter names" tripped a guard forbidding front-matter reads. I fixed it by rewording to name phases literally (edit applied).

Last batch (results received, not yet acted on): substrate-amendments surface at line 47–51 is `handlers(spineTools(db))` unpinned; `src/preview/example.ts:178` and `tests/support/self-hosting.js:282` also set verified_at; `tests/support/body-reads.js` has prose about verified_at reading; `check_coverage` referenced in tests/support/body-reads.js, tests/coverage-check-tool.test.js, tests/tool-naming.test.js; many suites sweep `inputSchema.properties` (conformance.test.js etc.).

So the immediate state: follow-ups 2–4 implemented (wording fix applied but not yet re-verified by a test run), follow-up 1 (verified_at) researched but not yet implemented.
</analysis>

<summary>
1. **Primary Request and Intent:**

   **Background.** Repo `/Users/chris/Work/git/pi-dpm` ports opencode-dpm to pi. Chris tests dpm skills against a local MTPLX server (port 8000, 8-bit Quality build) through a scratchpad RPC driver that auto-answers gates.

   **Requests this segment, in order:**
   - (carried over) Make dpm-do run exactly one epic, then end and offer the next epic for the user to start.
   - "Let's try this." — approval for a live dpm-do run.
   - "You can add the bash permission rule" — after the launch was blocked by the permission classifier.
   - "Make a note for the model to write verified_at" + mid-turn correction "Sorry tool" (i.e. the **tool**, not the model, should write `verified_at`).
   - "Something is still running. Can you check the fans?" → diagnosis of fans stuck at maximum.
   - "I also don't believe that Claude code kills other apps. If it need the only way it could have done it is through your intervention" — correction of my claim about the kill.
   - "Okay let's go again" — restart the server and resume the run.
   - "Let's keep monitoring."
   - "How do we actually clear context in our dpm toolset?" — explanation request.
   - "Do the follow ups" — build the four findings from the run.

   **Standing constraints (still in effect):**
   - Use the Edit tool for existing files; no sed/perl/awk edits (read-only use of grep/sed for inspection is fine).
   - Commit or push only when asked (`/commitp` = commit and push everything with `git add -A`); tags stay local.
   - Don't point load generators at :8000 while Chris is working; ask before measurement runs.
   - Don't edit `~/.pi/agent/models.json` without asking.
   - Don't restart or change the MTPLX server or config without asking.
   - Posting to MTPLX #277 needs Chris's go-ahead.
   - pi-dpm may diverge from opencode-dpm.
   - Address the user as Chris; use they/them.
   - Monitor long runs actively and report progress (memory `monitor-long-runs`).
   - Ask before launching runs; don't edit extension files while a run is live.

2. **Key Technical Concepts:**
   - **dpm handoff mechanism** (`extensions/dpm/handoff.ts`): tool marks, command carries out. `handoff` returns `terminate: true` (ends pi's agent loop before the next request); `tool_call` handler blocks later calls with `ALREADY_HANDED_OFF` (also terminating); `turn_end` aborts only when handoff shared a message; on `agent_settled` the extension calls `pi.sendUserMessage('/dpm-continue <skill> <args>')` via `setTimeout`; the command uses `ctx.newSession` (command-only in pi), injects a hidden `dpm-handoff` note naming the predecessor harness session id, then sends the skill invocation unawaited. Continuity lives in `session.state` JSON + typed rows, not prose.
   - **dpm-do phases:** `select load start plan work verify refactor done next summary`, plus `complete`.
   - **Session chain:** each handoff creates a successor session; predecessor gets `superseded_by`; `dpm_adopt_session({id, predecessor_id, include_body})`.
   - **MTPLX serve flags:** `--port 8000 --no-auth --fan-mode smart --enable-thermal-poll --preserve-thinking auto --tool-prompt-mode native --model mtplx-qwen38-27b-optimized-quality-fp16`; env `MTPLX_SESSION_BANK_MAX_BYTES=5G MTPLX_SESSION_BANK_PER_SESSION_BYTES=5G`. `--fan-mode smart` forces fans while generating and restores them only on clean shutdown.
   - **Tool conventions:** `SUPPLIED = { ulid, clock, allocated, derived(from) }` in `src/tools/convention.ts:134`; `DEFAULT_LIMIT = 50`; `spineTools(db, { now = () => new Date().toISOString(), newId = ulid, root = '.', skew, stamp })`; `serverSupplied` declares server-set columns; `defineTool({name, table, description, reads, mutates, derived, serverSupplied, inputSchema, handler})`.
   - **Coverage verification:** `verified_at` + `binding_hash` set together (table CHECK); hash computed server-side from stored row texts; editing either bound text clears the pair by trigger; `retire_coverage` keeps `verified_at`.

3. **Files and Code Sections:**

   - **`skills/dpm-do/SKILL.md`** (modified, uncommitted) — the single-epic behaviour plus three follow-up edits.
     - Input: "The epic, once resolved, holds for the whole run. **One run works one epic** and ends at Step 8; the next epic is a run of its own, started when the user asks for it."
     - Step 8 end: "**Then the run ends.** `dpm_update_session` with `phase: 'complete'`, so no later run is offered this one to resume. Name the next ready epic from `dpm_list_epic` with `ready: true` — its title and reference, as `/dpm-do` would take it — or say that none is ready and why. **This run does not work it, and does not ask whether to**: the user starts that run when they want it, and it opens a session of its own. The same holds when this epic stays open — a story still pending, or a retired one waiting on its question — and the report says why it stays open."
     - Guidelines: "...unmet criteria, an unroutable tag, a blocker, an ambiguous criterion, a change moment, and a retired story at Step 8. It ends at Step 8, after one epic, naming the next rather than asking to start it."
     - Step 8 count (follow-up 4): "**Say what the count is, and take it from `dpm_check_coverage`** with this spec's `spec_id` and this epic's `epic_id`. Its `epic` block counts the epic's live bindings, how many carry a ✓, and both again per requirement. **Do not add up the `dpm_list_coverage` pages by hand**: a run that did reported \"12 of the 12 bindings\" for an epic whose rows numbered thirteen..."
     - Step 6 Session (follow-up 2, after the fix): "`state`. **The `phase` is the step the run is actually in** — `verify` while verifying, `refactor` during the pass, `done` at the close. A run that did the work and left the phase at `work` throughout leaves nothing behind saying the verification happened, and the phase is the only place a stopped run's successor reads it: the first MTPLX dpm-do run was killed mid-story, and what it had recorded was what its continuation could resume from."
     - Step 4 (follow-up 3): "**Scratch files stay in the project and do not outlive the check they were made for.** ... A run that wrote its check binary and three sample CSVs into `/tmp` left them on a machine nobody would think to look at..."

   - **`src/tools/cross/coverage-check.ts`** (modified, uncommitted) — follow-up 4 code.
     - SELECT now includes `coverage.verified_at`.
     - `export function coverageReport(db: DatabaseSync, specId: string, epicId: string | null = null)`.
     - New block before `const gaps = [`:
       ```ts
       const epicScope = epicId === null ? null : (() => {
         const epic = db.prepare(`SELECT id, slug, parent_id FROM document
                                   WHERE id = ? AND kind = 'epic'`).get(epicId) as Row | undefined;
         if (!epic) throw new ToolError(`check_coverage: epic_id '${epicId}' names no epic`);
         if (epic.parent_id !== specId) {
           throw new ToolError(`check_coverage: epic '${epic.slug}' (${epicId}) is not an epic of this spec`);
         }
         const mine = coverage.filter((row) => located(row.story_criterion_id).epic === epic.slug);
         const byLabel = new Map<string, { label: string; bindings: number; verified: number }>();
         for (const row of mine) {
           const label = requirements.find((requirement) => requirement.id === row.requirement_id)?.label ?? row.requirement_id;
           const seen = byLabel.get(label) ?? { label, bindings: 0, verified: 0 };
           byLabel.set(label, { label, bindings: seen.bindings + 1, verified: seen.verified + (row.verified_at === null ? 0 : 1) });
         }
         return { id: epic.id, slug: epic.slug,
           stories: stories.filter((story) => story.epic === epic.slug).length,
           bindings: mine.length,
           verified: mine.filter((row) => row.verified_at !== null).length,
           by_requirement: [...byLabel.values()] };
       })();
       ```
     - Return gains `epic: epicScope,` after `untagged_criteria`.
     - inputSchema gains optional `epic_id`; `handler: (args) => coverageReport(db, args.spec_id, args.epic_id ?? null)`.
     - Docblock gains a paragraph about the 12-vs-13 miscount and why standings stay spec-wide.

   - **`tests/coverage-check-tool.test.js`** (modified) — two new tests inserted before `test('check_coverage refuses an id that is not a spec...')`: one asserting `{bindings: 1, verified: 0, stories: 2}` for `core`, `by_requirement` `[{label:'FR1',bindings:1,verified:0}]`, verified→1 after `update_coverage({id: binding.id, verified_at: AT})`, `extras` bindings 0, standings/gaps unchanged, and `whole.epic === null`; one asserting the two refusals.

   - **`tests/skill-do.test.js`** (modified) — new test 'a run works one epic, closes its session, and names the next epic without working it' checking the Input line, Step 8's "**Then the run ends.**", the "does not work it" clause, and that Guidelines no longer contains 'epic-end offer'.

   - **`src/tools/spine/coverage.ts`** (read, NOT yet modified) — follow-up 1 target. Docblock currently argues caller-supplied `verified_at`. `STATE = { position: {...}, verified_at: { type: 'string', description: 'ISO 8601. Records the ✓; ...' } }` at 131–137. `create_coverage` handler sets `verified_at: args.verified_at ?? null` and `binding_hash: args.verified_at == null ? null : bindingHash(db, args as Binding)`. `update_coverage` handler: if `changes.verified_at === undefined` plain update; else hash off the stored row or null. `retire_coverage` shows the precedent: `serverSupplied: { retired_at: SUPPLIED.clock }` + `retired_at: now()`.

   - **`extensions/dpm/handoff.ts`** (read) — source for the context-clearing explanation; key lines: `:143` registerTool, `:160` `terminate: true`, `:167` block, `:180` agent_settled, `:207` `ctx.newSession`, `:102` continuation note.

   - **`/private/tmp/claude-501/-Users-chris-Work-git-pi-dpm/5774d35e-880f-4bbc-8a79-499c991a65e7/scratchpad/mtplx-do1/`** — run directory: `project/` (copy of epics7 project + git), `home/`, `drive.mjs` (built-in tools enabled, bash logging added), `monitor.sh`, `out/` (part 1), `out2/` (resume).

   - **`/Users/chris/.claude/projects/-Users-chris-Work-git-pi-dpm/memory/dpm-do-run-followups.md`** (created/updated) — holds all four findings; MEMORY.md has the index line "- [dpm-do run follow-ups](dpm-do-run-followups.md) — after mtplx-do1: the tool, not the model, stamps coverage verified_at".

   - **`/Users/chris/Work/git/pi-dpm/.claude/settings.local.json`** (created, untracked) — single allow rule for `Bash(node /private/tmp/.../mtplx-do1/drive.mjs:*)`. Would be picked up by `/commitp`.

4. **Errors and fixes:**
   - **Launch blocked by the auto-mode permission classifier.** I stopped, explained, and offered the `!` form; Chris said to add a permission rule, which I did in `.claude/settings.local.json`.
   - **Go not installed** while the spec pins Go. Chris chose "Let the run install it"; the run hit a proper blocker gate and ran `brew install go` itself (Go 1.27.1).
   - **Both background tasks killed by Claude Code's low-memory handling** mid-story-2. I first wrote that "Claude Code killed the MTPLX server"; Chris pushed back ("I also don't believe that Claude code kills other apps"). I verified with `log show` (no jetsam entries) and corrected: only my own background tasks were killed, and MTPLX was exposed only because I ran it as one. Offered to let Chris own the server process in future.
   - **Fans stuck at maximum** after the kill. Diagnosed via Stats' bundled `smc`: both fans `forced` at max with cool sensors; cause was MTPLX's `--fan-mode smart` never restoring on a hard kill (Chris independently reached the same conclusion). Fixed by Chris running `sudo /Applications/Stats.app/Contents/Resources/smc reset`; verified `automatic` afterwards. My earlier `--samplers smc` suggestion was wrong on Apple Silicon (no such sampler) — corrected.
   - **Two `edit` tool mismatches on `ingest.go`** during the run (model editing from a stale copy); it recovered by re-reading.
   - **Summary miscount by the run**: reported "12 of the 12 bindings"/"6 under FR3"; DB says 13 total (FR4 1, FR5 1, FR7 1, FR3 7, NFR2 3), all verified. Recorded as follow-up 4 and fixed by the `check_coverage` epic scope + Step 8 wording.
   - **My Step 6 wording broke a test**: 'the skill recovers nothing by reading a generated file' failed because I wrote "from the list this skill's front matter names". Fixed by rewording to name the phases literally ("**The `phase` is the step the run is actually in** — `verify` while verifying, `refactor` during the pass, `done` at the close"). Not yet re-run.

5. **Problem Solving:**
   - The single-epic change works end to end: Step 8 closed epic 01-01 (`dpm_update_epic status: 'complete'`), claimed FR4 and NFR2 (`coverage_claimed_at` from a real `date -u` call, `2026-09-16T06:53:32Z`), set session `phase: 'complete'`, named `01-02` "CSV ingest" and said "this run is finished", and the process exited 0 without starting the next epic.
   - Per-story handoffs worked twice, plus an unplanned third recovery: after the hard kill, a new process with prompt `/dpm-do` listed sessions, adopted `01a0a684` (phase `verify`, `story_in_flight` intact), re-ran `go test`, saw the 10 bindings already stamped (no duplicate writes), and continued.
   - Whole run: 2 gates (both legitimate — the listed-draft guard and the Go blocker), 5 recovered tool errors in part 1 and 0 in part 2, no model errors, context peaked ~43k tokens (vs ~86k OOM point).
   - Paging ruled out as the miscount cause (`DEFAULT_LIMIT = 50` vs 7 rows).

6. **All user messages:**
   - "Let’s try this."
   - (AskUserQuestion answers) "Let the run install it" / "Let dpm-do pick"
   - "You can add the bash permission rule"
   - "Make a note for the model to write verified_at"
   - "Sorry tool" (mid-turn correction: the tool should write verified_at)
   - "Something is still running.  Can you check the fans?"
   - "powermetrics: unrecognized sampler: smc"
   - (pasted readings) "CPU Power: 446 mW / GPU Power: 213 mW / Combined Power (CPU + GPU + ANE): 659 mW / **** Thermal pressure **** / Current pressure level: Nominal / GPU HW active residency:  37.92% (389 MHz:  38% ...) / GPU Power: 213 mW"
   - "fans are still running"
   - "i've install stats"
   - "done, fans are quiet now"
   - "Okay let’s go again"
   - (AskUserQuestion answers) "Keep --fan-mode smart" / "Restart with 5G bank"
   - "I also don’t believe that Claude code kills other apps.  If it need the only way it could have done it is through your intervention"
   - "I think the fans were left such because mtplx was killed rather than shutdown properly"
   - "Let’s keep monitoring" / "Okay, let’s keep monitoring"
   - "How do we actually clear context in our dpm toolset?"
   - "That’s fascinating because in Claude code it can only be done by the user manually"
   - "Do the follow ups"

7. **Pending Tasks:**
   - **Follow-up 1 (not started):** make the tool, not the model, stamp `verified_at`. Planned shape: `create_coverage`/`update_coverage` accept `verified: boolean` (true → `now()`, false → clear both), declare `serverSupplied: { verified_at: SUPPLIED.clock }`, drop `verified_at` from the caller schema, rewrite the module docblock (which currently argues the opposite). Affected suites and call sites: `tools.test.js` (surface pins `now: () => STAMP` = '2026-08-08T12:00:00.000Z'; lines 394, 395, 419), `coverage-retirement-tool.test.js` (103, 193; surface pins `AT`), `criterion-supersession.test.js` (56; unpinned), `retired-claim.test.js` (79; pinned), `skill-do.test.js` (257; surface line 299 unpinned; exact assertion line 330), `skill-inspect.test.js` (112), `skill-status.test.js` (127), `skills-corpus.test.js` (268; surface line 281 unpinned; exact assertion line 319), `supersession-retirement.test.js` (69; surface lines 33–37 unpinned; exact assertion line 219), `substrate-amendments.test.js` (380, 388, 395; surface lines 47–51 unpinned; exact assertion 398). Also `src/preview/example.ts:178` and `tests/support/self-hosting.js:282` set `verified_at`; suites sweeping `inputSchema.properties` (conformance.test.js and others) may need checking. Tests that seed rows directly (decay, delivery, integration, coverage-retirement-migration) are unaffected.
   - Re-run `npm test` (full suite was 1282 tests before these changes; new tests added since).
   - Nothing is committed; commit only if Chris asks. `.claude/settings.local.json` is untracked and would be swept up by `/commitp`.
   - The MTPLX server is still running as my background task (`b2hle92sg`); stopping it cleanly with TaskStop returns the fans properly.

8. **Current Work:**
   Implementing the four follow-ups after Chris said "Do the follow ups". Follow-ups 2, 3 and 4 are edited into `skills/dpm-do/SKILL.md`, `src/tools/cross/coverage-check.ts` and `tests/coverage-check-tool.test.js`. A test run of `tests/coverage-check-tool.test.js tests/skill-do.test.js` produced one failure — 'the skill recovers nothing by reading a generated file' — caused by my Step 6 phrase "from the list this skill's front matter names"; I applied the reword and have not yet re-run the suites. The last tool batch returned: `substrate-amendments.test.js` surface is `const surface = (t) => { const db = planning(t); return { db, call: handlers(spineTools(db)) }; };` (unpinned, lines 47–51); other `verified_at` users are `src/preview/example.ts:178` and `tests/support/self-hosting.js:282`; `check_coverage` is referenced in `tests/support/body-reads.js`, `tests/coverage-check-tool.test.js` and `tests/tool-naming.test.js`; many suites sweep `inputSchema.properties`.

9. **Optional Next Step:**
   Re-run `node --test tests/coverage-check-tool.test.js tests/skill-do.test.js` to confirm the reworded Step 6 paragraph and the new `check_coverage` epic scope pass, then implement follow-up 1 (`verified: boolean` + `serverSupplied: { verified_at: SUPPLIED.clock }` in `src/tools/spine/coverage.ts`) and update the ten affected test suites, finishing with the full `npm test`. This follows directly from Chris's "Do the follow ups" and from my own statement of the plan: "Follow-up 1 is the big one: making the tool stamp `verified_at` reverses what the module docblock currently argues ("`verified_at` is the caller's") and touches ten test suites. I'll implement it as a `verified: true` argument with `serverSupplied: { verified_at: SUPPLIED.clock }`, matching `retire_coverage`." Do not commit unless asked.
</summary>
