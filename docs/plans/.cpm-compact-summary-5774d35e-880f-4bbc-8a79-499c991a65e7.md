# Compact Summary (PostCompact)
**Captured**: 2026-09-13 20:00:20
**Trigger**: auto

<analysis>
**Chronology after the previous summary**

1. **Cache diagnosis for run 5 (per-step thinking).**
   - Found the Qwen template's effort sentence at the start of the system prompt. `xhigh`/`low` add it; `medium` adds nothing.
   - MTPLX maps pi's `high` up to `xhigh`.
   - MTPLX renders history differently with thinking on vs off (`openai.py:13909`).
   - So every level change broke the prefix cache.
   - Reported to Chris and recommended one level per skill.

2. **Chris: "Stop and fix thinking per skill".**
   - Killed run 5.
   - Rewrote `phases.ts` so the `Plan` is `{level, phases: string[]}`. `thinking:` is required on every skill; `phases:` lists ids only.
   - Edited 16 `SKILL.md` files, the convention sentence and the tests.
   - The test uses `--thinking low`, because project `settings.json` `defaultThinkingLevel` didn't take effect.
   - All 1246 tests passed.

3. **Level changes.** Chris: "Switch high to medium" (architect, audit, inspect, review). Then "Move library to low".

4. **Chris: "Yes".** Committed `99b2020` and pushed. Started run 6 with a monitor.
   - Run 6 was killed by the harness for low memory at 977s.

5. **Memory.** Chris sent an Activity Monitor screenshot: `python3.14` (MTPLX) at 44.85 GB.
   - I suggested `--memory-budget`; Chris said "--memory-budget is not a valid setting".
   - Found the `mtplx config` key `ram_session_cache_max_size` (dry run accepted 8GB) and the env var `MTPLX_SESSION_BANK_MAX_BYTES`.
   - Chris restarted MTPLX; the log showed "session-bank budget: 8.0G total (explicit)".

6. **Run 6b started. Chris asked "what does is session bank setting used for?"** Explained.

7. **Chris: "Can you get Activity Monitor figures".**
   - `footprint` showed 35 GB (peak 37 GB), later 32 GB (peak 39, then 47 GB).

8. **Run 6b monitoring.**
   - The 538s cache "miss" was an MTPLX "orphaned" request with no usage reported, not a real miss.
   - `retokenized_history_mismatch` means the postcommit isn't stored, so the last turn is reprocessed each time.
   - Run 6b completed at 4444s.
     - 18 gates; 2 guard blocks at 796s and 2571s (missed by the monitor, whose grep pattern was wrong).
     - thinking 76k chars; cache 3.29M read vs 333k new.
     - 19 requirements, 3 ADRs, 8 options, 24 trade-offs, 16 criteria, 18 approaches; phases in order through `complete`.

9. **Chris: "Can you review the quality of each completed spec"** (runs 1, 2, 6b).
   - Dumped the DBs to `scratchpad/review/run1.txt`, `run2.txt`, `run6b.txt` and reviewed them.
   - Key cross-cutting finding: must-not criterion text is broken in all three.
     - The template prefixes "must NOT — " (`src/projection/templates/spec.ts:58`).
     - Runs 1 and 6b wrote "does not" (double negative). Run 2 cut "must not" out, leaving "tally print…".

10. **Chris: "Make the change".**
    - Edited the `criterion.ts` description, `dpm-spec` `SKILL.md` and `dpm-epics` `SKILL.md`.
    - The `tool-naming` test failed because descriptions are pinned to the v0.7.0 oracle; added a `REWORDED` entry and it passed.
    - Full suite before the `REWORDED` addition: 1245/1246, the only failure being that test. Typecheck and skills check passed.
    - Not committed (asked Chris; no answer yet).

11. **Chris asked "Did run 6b have thinking on?" twice.** Answered yes, `medium`, with evidence.

12. **Chris: "Show me the spec for 6b".**
    - Rendered via `renderDocument` into `scratchpad/review/01-spec-tally-cli.md` and showed it in full.

13. **Chris: "Can you run dpm-publish for 6b?"**
    - Followed the skill: dry run via a script (2 new, 0 removed), then `node bin/dpm-publish.ts <project>`.
    - Wrote `docs/specifications/01-spec-tally-cli.md` (identical to the earlier render), `.dpm/dpm.sql` and `.dpm/dpm.db.synced`.

14. **Chris: "Can we try dpm-epics with 6b. Monitor as before".**
    - Created `scratchpad/mtplx-epics1/drive.mjs` (prompt from argv, 180-minute limit, gate cap 80), copied the project and home, and started with a prompt naming spec 01. Monitor `bretn1a9t` started.

15. **Chris: "dpm-epics shouldn't ordinarily need prompt as it should examine the database to see what's needed".**
    - Read the epics Input section: with no argument it calls `dpm_list_spec` and offers a gate.
    - Killed the first run (driver 59437 and its pi child) and reset `project` and `out` from `mtplx-run6b/project`: session count 1 (complete), 0 epics.
    - Started bare `/dpm-epics` (background task `bd2qzf1ep`); driver pid 60707, pi child 60708.
    - The monitor `bretn1a9t` is still armed on the same `gates.md` path.
    - The check showed only the new processes, with no orphans. pid 66637 is Chris's own pi, not matched this time.

**Standing constraints:**
- Ask before each measurement run.
- Use the Edit tool for existing files (no sed/perl/awk edits).
- Commit or push only when asked.
- Tags stay local.
- Don't edit `~/.pi/agent/models.json` without asking.
- pi-dpm may diverge from opencode-dpm.
- Don't restart or change Chris's MTPLX server or config without asking.
</analysis>

<summary>
1. Primary Request and Intent:
   - **Background:**
     - Repo `/Users/chris/Work/git/pi-dpm` (GitHub `ninthspace/pi-dpm`) ports opencode-dpm to pi 0.85.1.
     - Spec artifact: https://claude.ai/code/artifact/5b8cec6e-5856-4ad5-87e4-d608d458c8bd
     - Chris runs local model tests against MTPLX (Qwen3.8 27B) on `:8000`.
   - **Requests in this segment, in order:**
     - "Stop and fix thinking per skill"
     - "Switch high to medium"
     - "Move library to low"
     - "Yes" (commit, push, start run 6)
     - "--memory-budget is not a valid setting"
     - Chris pasted the MTPLX startup log after restarting with the 8G cache cap.
     - "what does is session bank setting used for?"
     - "Can you get Activity Monitor figures"
     - "Can you review the quality of each completed spec"
     - "Make the change" (fix the must-not wording)
     - "Did run 6b have thinking on?" (asked twice)
     - "Show me the spec for 6b"
     - "Can you run dpm-publish for 6b?"
     - "Can we try dpm-epics with 6b. Monitor as before"
     - "dpm-epics shouldn't ordinarily need prompt as it should examine the database to see what's needed"

2. Key Technical Concepts:
   - **MTPLX prefix cache and thinking changes:**
     - The Qwen 3.8 template writes an effort sentence for `xhigh` and `low` (nothing for `medium`) at the very start of the system prompt.
     - pi's `high` is mapped up to `xhigh`.
     - Thinking on vs off changes how the history is rendered (`include_reasoning`, `openai.py:13909`).
     - So any level change invalidates the cache: run 5 reprocessed 25,456 tokens in 308s at one switch and 46,558 in 794s at another.
   - **Per-skill thinking:** the level is set once at skill activation, when the context is small; phases no longer change it.
   - **pi thinking level:**
     - `--thinking low` works as a CLI flag.
     - Project `.pi/settings.json` `defaultThinkingLevel` did not take effect in tests.
     - The SDK resolves `options.thinkingLevel`, then the per-model setting, then `settingsManager.getDefaultThinkingLevel()`.
   - **MTPLX memory:**
     - Session bank auto budget is 0.5 × (RAM − weights) (about 22.5 GB on 64 GB).
     - Controlled by `ram_session_cache_max_size` in `mtplx config` or `MTPLX_SESSION_BANK_MAX_BYTES`. `--memory-budget` exists only on the internal server module (`MTPLX_MEMORY_BUDGET` env).
     - Chris set 8GB; the startup log shows "session-bank budget: 8.0G total (explicit), 8.0G per-session cap, 24 entries max, model weights 19.3G".
     - SSD session cache: 100GB.
     - `footprint <pid>` gives the Activity Monitor figure.
   - **MTPLX logs:**
     - `~/.mtplx/logs/request-log-8000.jsonl` has `prompt_tokens`, `cached_tokens`, `cache_source`, `cache_miss_reason`, `cancellation_reason`.
     - `~/.mtplx/metrics/flight-8000.jsonl` has `ev:"end"` with `reason` (`stop`/`orphaned`) and `ev:"pc"` postcommit with `retokenized_history_mismatch` vs `generation_boundary_prefix_of_history`.
     - An "orphaned" request has no usage, so pi shows `cacheRead 0` even though the cache was used.
     - `retokenized_history_mismatch`: rendered history tokens ≠ prompt + generated, so the entry isn't stored and the next request reprocesses the last turn (about 400–3,300 tokens).
   - **dpm projection:**
     - `renderDocument(db, id)` returns `{path, text, kind}`.
     - `publish(db, {root, dryRun})` plus `describe(record)`; `bin/dpm-publish.ts <root>`.
     - The spec template writes a `must NOT — ` prefix for `polarity: 'must_not'`, so the text should name the rejected outcome as though it happened.
   - **Tool surface oracle:** `tests/fixtures/v070-tool-surface.json` must never be rewritten (`parity-v070.test.js`). Deliberate rewordings are declared in `REWORDED` in `tests/tool-naming.test.js`, with `tools`, `at`, `was`, `now` and `why`.

3. Files and Code Sections:
   - **`extensions/dpm/phases.ts`** (rewritten; committed in `99b2020`)
     - Module doc explains per-skill levels and the run 5 cache evidence.
     - Exports: `ThinkingLevel`, `LEVELS`, `FINAL='complete'`, `MOVES`, `ADOPT`, and `type Plan = { readonly level: ThinkingLevel; readonly phases: readonly string[] }`.
     - `planFor`:
       - `thinking` is required (throws "declares phases and no thinking level");
       - throws on a bad level;
       - a phase id containing `:` throws "carries a level; the skill has one, in thinking";
       - throws when `complete` is declared, on repeated ids, and on an empty phases list.
     - `plans`, `listed`, `phaseNote`, `unknownPhase` and `reminder` are unchanged in behaviour; `reminder` now uses string ids and `indexOf`.
     - `registerPhases`:
       - the `tool_call` guard on unknown ids is kept;
       - the `tool_result` gate reminder is kept;
       - on a successful move or adopt it only sets `current` and no longer changes the level;
       - `activate` calls `pi.setThinkingLevel(plan.level)` and returns `phaseNote`.
   - **`skills/*/SKILL.md` front matter** (committed `99b2020`)
     - Every skill has `thinking:`. Phased skills also have `phases:` with ids only.
     - Final levels:
       - off: artifact, publish, status, templates
       - low: clean, ralph, library
       - medium: every other skill (16), including architect, audit, inspect and review
   - **`shared/skill-conventions.md`:** "Move it at every step, including steps whose work looks like the last one's: each answered gate is reminded of the phase last recorded, and a phase left behind points that reminder at a step the run has already finished."
   - **`tests/pi-phases.test.js`** (rewritten, committed)
     - `PROJECT_LEVEL='low'` and `args = ['--skill', SKILLS, '--thinking', PROJECT_LEVEL]`.
     - Tests:
       - declarations;
       - `planFor` acceptance and refusals;
       - note;
       - reminder;
       - integration: every request at the skill's level `[level, level, level]`, with `notEqual(level, PROJECT_LEVEL)`;
       - reminder plus `complete` with all requests at level;
       - control: requests are `[PROJECT_LEVEL, PROJECT_LEVEL]`.
   - **`src/tools/spine/criterion.ts`** (uncommitted). The polarity description is now:
     ```ts
     description: "'must_not' is a type here, and the document writes 'must NOT — ' before the text, so "
       + "the text names the rejected outcome as though it happened: 'a raw stack trace reaches the user', "
       + "not 'the tool does not print a stack trace' and not 'must NOT print a stack trace'",
     ```
   - **`skills/dpm-spec/SKILL.md`** (uncommitted). A new paragraph was added after the must-not probe:
     ```
     **A must-not criterion's text names the rejected outcome as though it happened.** The spec document
     writes "must NOT — " in front of it, so the text supplies only what is rejected:

     - *a raw stack trace reaches the user* reads "must NOT — a raw stack trace reaches the user";
     - *tally does not print a stack trace* reads as a double negative;
     - *tally print a stack trace*, with "must not" cut from the middle, reads as neither.
     ```
   - **`skills/dpm-epics/SKILL.md`** (uncommitted). This sentence was appended: "The document writes "must NOT — " in front of it, so the text names the rejected outcome as though it happened: *a credential reaches a log line*, not *no credential is logged*, which would read as a double negative."
   - **`tests/tool-naming.test.js`** (uncommitted). A `REWORDED` entry was added:
     - `tools`: `create_acceptance_criterion`, `create_story_criterion`, `update_acceptance_criterion`, `update_story_criterion`
     - `at`: `['inputSchema','properties','polarity','description']`
     - `was`: "'must_not' is a type here, not the words 'must NOT' at the front of the text"
     - `now`: the new description, exactly as above
     - `why`: 'it forbade the prefix without saying what the text is, and specs rendered as double negatives'
   - **Scratch** (`/private/tmp/claude-501/-Users-chris-Work-git-pi-dpm/5774d35e-880f-4bbc-8a79-499c991a65e7/scratchpad`):
     - `mtplx-run3/drive.mjs`: the spec driver.
       - The prompt is hard-coded to `/dpm-spec` for the tally CSV tool; limit 120 minutes; gate cap 60.
       - It writes `out/events.jsonl` and `out/gates.md`.
       - Error lines have the form `- Ns tool X **ERROR** text`.
     - `mtplx-run6b/`: the completed spec run.
       - `project/.dpm/dpm.db`;
       - after publish: `docs/specifications/01-spec-tally-cli.md`, `.dpm/dpm.sql`, `.dpm/dpm.db.synced`.
     - `review/`:
       - `run1.txt`, `run2.txt`, `run6b.txt` (DB dumps);
       - `render.ts`;
       - `run6b-copy.db`, `run6b-spec.md`;
       - `01-spec-tally-cli.md`;
       - `publish-preview.ts`.
     - `mtplx-epics1/drive.mjs`: a copy of the driver with `const [, , project, home, out, PROMPT] = process.argv;`, `LIMIT_MS = 180*60*1000`, gate cap 80, and header `# dpm skill on MTPLX via pi`.
     - `mtplx-epics1/home/`: `auth.json`, `models-store.json` and `models.json` copied from run 6b.
     - `mtplx-epics1/project/`: a fresh copy of `mtplx-run6b/project`.
     - `mtplx-epics1/out/`: the current run's logs.
     - Run dbs: `mtplx-run` (run 1, complete), `mtplx-run2` (complete), `mtplx-run3`–`mtplx-run6` (incomplete).

4. Errors and fixes:
   - **Control test failed with the project's `.pi/settings.json` `defaultThinkingLevel: 'low'`:** requests came out `medium`. Fixed by using `--thinking low` in the test's `args`.
   - **Run 6 killed by the harness for low memory:** MTPLX was at 44.85 GB. I suggested `--memory-budget`, which Chris said is invalid. The correct setting is `ram_session_cache_max_size` (Chris set 8GB and restarted).
   - **Wrong claim "no guard blocks" for run 6b:** the monitor grepped `error: `, which `gates.md` lines don't contain. Corrected in the report (2 blocks). The new epics monitor matches `\*\*ERROR\*\*|extension_error|refused `.
   - **Wrong claim about the heartbeat's "2 errors" in the killed run 6:** it counted the word "error" inside requirement text. Clarified.
   - **Cache "misses" (259/269s in run 6, 538s in run 6b):** MTPLX orphaned requests with no usage, not real misses.
   - **`tool-naming` test failed after the description change:** the descriptions are pinned to the v0.7.0 oracle. Fixed with a `REWORDED` entry; that test then passed 4/4.
   - **`PIPESTATUS` printed empty:** zsh. Verified the publish output by files and diff instead.
   - **Chris's feedback that `dpm-epics` shouldn't need a prompt:** killed the first epics run, reset the directory, restarted with bare `/dpm-epics`.

5. Problem Solving:
   - **Run 6b outcome:**
     - `dpm-spec` completed on MTPLX in 4444s with one thinking switch (`medium` at 0s).
     - 18 gates, all with text; 2 guard blocks, both retried successfully.
     - All 12 phases recorded in order, then `complete`.
     - No invented option ids.
   - **Quality review:**
     - Run 1: best ADR reasoning. Flaws: the ENV5 vs ADR1 contradiction, no money precision or date format, overlapping ADRs, performance test tagged `unit`.
     - Run 2: most precise requirements (decimal, locale). Flaws: 6 must-not texts missing their verb, duplicate ENVX1/ENVX4, the Go option straw-manned, no cost axis on ADR3, rounding unspecified.
     - Run 6b: most consistent, best criteria. Flaws:
       - money precision never decided;
       - thin trade-offs;
       - filler ADR 2;
       - an exit-code-3 criterion adds scope;
       - no criterion tagged `integration` despite the integration section recommending it;
       - FR3 criteria miss unreadable and empty files.
     - Shared: must-not double negatives or broken grammar; should/could requirements have no criteria; refunds and negative amounts are unaddressed.

6. All user messages:
   - "Stop and fix thinking per skill"
   - "Switch high to medium"
   - "Move library to low"
   - "Yes"
   - [screenshot of Activity Monitor: python3.14 44.85 GB, etc.]
   - "--memory-budget is not a valid setting"
   - [pasted MTPLX startup log showing session-bank budget 8.0G explicit]
   - "what does is session bank setting used for?"
   - "Can you get Activity Monitor figures"
   - "Can you review the quality of each completed spec"
   - "Make the change"
   - "Did run 6b have thinking on?" (sent mid-turn)
   - "Did run 6b have thinking on?"
   - "Show me the spec for 6b"
   - "Can you run dpm-publish for 6b?"
   - "Can we try dpm-epics with 6b. Monitor as before"
   - "dpm-epics shouldn't ordinarily need prompt as it should examine the database to see what's needed"
   - **Standing constraints from earlier:**
     - "Do not point load generators at `:8000` while Chris is working… **Ask before each measurement run.**"
     - Use the Edit tool for existing files (no sed/perl/awk edits).
     - Commit or push only when asked.
     - Tags stay local.
     - Don't edit Chris's `~/.pi/agent/models.json` without asking.
     - pi-dpm may diverge from opencode-dpm freely.
     - Don't restart or change Chris's MTPLX server or config without asking.

7. Pending Tasks:
   - Monitor the bare `/dpm-epics` run (background `bd2qzf1ep`, monitor `bretn1a9t`) and report each event: gates, phases, thinking, errors or blocks, finish.
   - When it finishes, review the epics output (epics, stories, tasks, story criteria, coverage) and summarise timing, gates, errors and cache.
   - The must-not wording fix is uncommitted (`src/tools/spine/criterion.ts`, `skills/dpm-spec/SKILL.md`, `skills/dpm-epics/SKILL.md`, `tests/tool-naming.test.js`). I asked Chris whether to commit and push; there's no answer yet, so don't commit until asked.
   - Carry-overs:
     - `DATABASE` uses the process cwd rather than `ctx.cwd`;
     - no db close on `session_shutdown`;
     - possibly lower maxTokens;
     - MTPLX `retokenized_history_mismatch` cost could be raised with MTPLX.

8. Current Work:
   - After Chris said "dpm-epics shouldn't ordinarily need prompt as it should examine the database to see what's needed", I:
     - confirmed from the Input section of `skills/dpm-epics/SKILL.md` that with no argument it calls `dpm_list_spec` and offers the specs with `question`;
     - killed the first epics run (driver 59437 and its pi child);
     - reset `mtplx-epics1/project` (fresh copy of `mtplx-run6b/project`: 1 session `complete`, 0 epics) and `mtplx-epics1/out`;
     - started:
     ```
     cd $S/mtplx-epics1/project && node $S/mtplx-epics1/drive.mjs $S/mtplx-epics1/project $S/mtplx-epics1/home $S/mtplx-epics1/out "/dpm-epics"
     ```
     This is background task `bd2qzf1ep`; driver pid 60707, pi child pid 60708. The process check showed no leftovers from the first run.
   - The monitor `bretn1a9t` is still armed, tailing `mtplx-epics1/out/gates.md` with the filter `^## Gate|phase "|thinking →|\*\*ERROR\*\*|extension_error|refused |finished:|pi exited`, plus a 15-minute heartbeat counting gates, tool calls and errors/blocks.
   - Nothing about the restart has been reported to Chris yet.

9. Optional Next Step:
   - Tell Chris the epics run was restarted as bare `/dpm-epics`, from a fresh copy of run 6b's project. With no argument the skill lists specs and offers them at a gate, which the driver answers with the first option.
   - Then continue as Chris asked ("Can we try dpm-epics with 6b. Monitor as before"): report each monitor event from `bretn1a9t`.
   - When `bd2qzf1ep` finishes, stop the monitor and review the epics output against run 6b's spec.
</summary>
