# Compact Summary (PostCompact)
**Captured**: 2026-09-16 11:12:13
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Segment 1 — continuation from a prior compacted session.** The conversation resumed mid-task: Chris had said "Do the follow ups" (four findings from the mtplx-do1 dpm-do run). Follow-ups 2, 3, 4 were already edited into `skills/dpm-do/SKILL.md`, `src/tools/cross/coverage-check.ts`, `tests/coverage-check-tool.test.js`. Follow-up 1 (make the tool, not the model, stamp `verified_at`) was not started. A test failure from my Step 6 wording had been reworded but not re-run.

**Segment 2 — verifying the reword and gathering for follow-up 1.** I re-ran `tests/coverage-check-tool.test.js tests/skill-do.test.js` → 27 tests, 0 failures. I then gathered: `src/preview/example.ts:178` (a `create_coverage` call with `verified_at`), `tests/support/self-hosting.js:282` (same), skills that mention `verified_at` (dpm-ralph, dpm-pivot, dpm-do, dpm-epics, dpm-status, dpm-inspect, shared/status-model.md), and the conformance sweep.

Key discovery sequence for follow-up 1 design:
- `SUPPLIED` in `src/tools/convention.ts:134` — `{ulid, clock, allocated, derived(from)}`.
- Conformance lives in `tests/support/conformance.js`, NOT src. Line ~110: "An argument may legitimately not be a column" — only `enum` arguments must name columns. So a `verified: boolean` argument raises no conformance problem. This was the blocking design question.
- `bindings()` in `tests/support/skills.js:775`, line 812: `if (!new RegExp(\`\\\`${argument}\\\\b\`).test(source))` — any argument a run passes to a create_/update_ tool must be named in the SKILL.md in a code span. `\b` fails between `verified` and `_at`, so `` `verified_at` `` does NOT satisfy a run passing `verified`. This made the dpm-do Step 5 rewrite mandatory and machine-checked.
- `spineTools(db, { now: () => X })` is an established idiom for pinning the clock (tools.test.js:25, coverage-retirement-tool.test.js:30, retired-claim.test.js:42, etc.).

**Segment 3 — implementing follow-up 1.** I made ~35 edits in one batch:
- `src/tools/spine/coverage.ts`: docblock rewritten (both halves server's, citing the MTPLX run's invented `verified_at: "2026-09-15T19:17:00Z"`); `STATE.verified_at` → `STATE.verified` boolean; `create_coverage` serverSupplied gains `verified_at: SUPPLIED.clock`; handler `verified_at: args.verified ? now() : null`, `binding_hash: args.verified ? bindingHash(db, args as Binding) : null`; `update_coverage` gains serverSupplied and handler destructures `({ id, verified, ...changes })`.
- `skills/dpm-do/SKILL.md` Step 5: "with `verified: true`" plus a paragraph about the MTPLX run supplying its own `verified_at`.
- Ten test suites updated: argument changes plus `now` pinning where exact stamps were asserted (substrate-amendments added `const AT = '2026-08-10T00:00:00.000Z'`; supersession-retirement, criterion-supersession pinned to their AT; skill-do and skills-corpus pinned to '2026-08-09T00:00:00.000Z'; skill-pivot pinned to VERIFIED at two surfaces).
- `tests/tools.test.js`: assertions became STAMP; added a check that `verified_at` is absent from both schemas.
- `src/preview/example.ts`, `tests/support/self-hosting.js`: `verified: true`.
- Added to skill-do.test.js: `assert.ok(!passed.get('update_coverage').has('verified_at'), ...)`.

**Segment 4 — full suite, 4 failures, both diagnosed.**
- Three body-reads failures: unclassified site `'dpm-do · list_coverage · 8. Epic summary #3'`, corpus 118 vs 117 entries. Cause was the *earlier* follow-up 4 edit adding a third `dpm_list_coverage` mention to Step 8. Fixed by rewriting `#2`'s reason for the new site and adding `#3` with the old "bindings still standing" reason. The quote rule is `entry.stepText.includes(phrase)` — step-scoped, not block-scoped.
- One parity failure: `create_coverage differs from what v0.7.0 advertised` at `tests/tool-naming.test.js:268`. The oracle (`tests/fixtures/v070-tool-surface.json`, 183 tools) may never be regenerated; `REWORDED` pins text at a path with `assert.equal`, so it cannot express a rename. I added a sibling `RESHAPED` list `{tools, from, to, now, why}`, extended `withRewordings` to apply renames, and added a validation loop asserting `from` exists in the oracle and `to` does not.
- Full suite then green: 1284 tests, 0 failures; `tsc --noEmit` clean; `npm run skills` and `npm run modules` clean.

**Segment 5 — memory update and /commitp.** I rewrote `dpm-do-run-followups.md` to record built work and updated MEMORY.md's index line. Chris invoked `/commitp`. I ran `git add -A`, committed `2a69c39` with the mandatory template, pushed to `main`. I also corrected myself: `.claude/settings.local.json` is matched by `**/.claude/settings.local.json` in `~/.gitignore`, so my earlier warning that it would be swept up was wrong.

**Segment 6 — "Okay let's continue with our dpm testing".** I checked state: MTPLX up (HTTP 200, pid 19929 under a Claude Code shell snapshot, i.e. my background task); `drive.mjs` runs pi with `--skill $REPO/skills -e $REPO/extensions/dpm` pointed at the repo; project epics: cli-contract complete, csv-ingest/aggregation-and-output/repo-and-env-baseline pending. I asked two questions via AskUserQuestion. Chris answered: "Next epic on the same project" and "Leave it as is" (server ownership).

**Segment 7 — the run.** Created `out3/`, loaded Monitor via ToolSearch, launched `node drive.mjs <project> <home> <out3> /dpm-do` in background (task bsq8i7u0m), armed a persistent Monitor (bute5twhg). Then a long sequence of monitor events which I reported on.

Key observations during the run:
- Gate 1 at 336s: three ready epics → skill asks (correct per Input step 2). Driver answered CSV ingest (01-02).
- `dpm_create_session` phase "select" at 355s — NOT adopt_session, validating the single-epic end.
- errors:1 at 480s was a benign project-type probe whose final `ls artisan 2>/dev/null && echo "LARAVEL"` exited 1.
- Phases: select(355) work(1236) work(1644) verify(2094) refactor(2324) select(5058) load(6434) work(6847) select(7444) summary(7681) complete(8237).
- Follow-up 1 confirmed at 2191s: `{"id":"...","verified":true}` → `verified_at=2026-09-16T08:07:11.212Z/.214Z/.215Z` with binding hashes.
- Slowdown diagnosed: prompt-cache misses (`cacheRead: 0`, input ~60-62k) cost ~15 min/turn; hits cost 16-98s.
- Handoff 1 at 5956s; context 63.3k → 20.2k in the successor.
- Handoff 2 at 7476s.
- `dpm_check_coverage` called at 7768s with `{spec_id, epic_id}`.
- `bash :: date -u` at 8131s → `coverage_claimed_at: "2026-09-16T09:46:10Z"` on 3 requirements.
- Run finished: `pi exited 0 at 8287s`.

**My three mid-run errors** (important): I three times inferred defects from the status line, which carries only the last tool call of a tick:
1. "task 2 closed on an edit alone with no test run" — false; `6886s bash :: go test -count=1 -run 'TestEmptyFileExitsOne' ... && go test ./...` ran between.
2. "story 2 may be verified on stale evidence" — false, same evidence.
3. "`dpm_check_coverage` never called" — false; it was called at 7768s but shared a timestamp with `dpm_list_taxonomy`.
I retracted each explicitly and stated the method correction.

**Segment 8 — scoring.** Three-way agreement: check_coverage epic block `{stories:2, bindings:14, verified:14, by_requirement:[FR1 5/5, FR3 8/8, FR9 1/1]}`, the database (same), and the run's summary sentence ("All 14 of the 14 bindings that remain ... FR1 5/5, FR3 8/8, FR9 1/1"). 14 coverage writes, 0 with `verified_at`, all `verified: true`. All writes inside `project/`. Phases half-fixed (story 2 recorded neither verify nor refactor). Run quality: 1 gate, 1 benign error, 0 blocked, 0 model errors, 2 handoffs.

Three new findings proposed: `coverage_claimed_at` still caller-supplied; second story skips phases; duplicate observations.

**Segment 9 — "Record and build the fix".** I issued three parallel items: grep for `coverage_claimed`, oracle check of `update_requirement`, and a Write rewriting the memory file. All three returned successfully:
- `src/tools/spine/requirement.ts:52` has `coverage_claimed_at:` in its schema; `:126` has `handler: ({ id, coverage_claimed_at: claimedAt, ...changes }) => {`.
- `src/coverage/claim.ts` has the claim logic (lines 92-138) including `coverage_claim_hash` — so there IS a paired hash, exactly like `binding_hash`.
- Oracle's `update_requirement` props: `id, label, class, moscow, exclusion, parent_id, text, position, coverage_claimed_at`; `coverage_claimed_at` description: "ISO 8601. Claims the bound coverage rows account for this requirement whole; the server computes the hash over the bound set that accompanies it"; `has coverage_claimed: false`. So a RESHAPED entry is viable.
- Test call sites needing updates: `tests/skill-do.test.js:286`, `tests/supersession-retirement.test.js:84,203`, `tests/retired-claim.test.js:84,130`. Direct-SQL sites (decay, integration, coverage-retirement-migration, retired-claim:234) are unaffected. `skills/dpm-do/SKILL.md:314` prescribes the argument and must be reworded (bindings() will enforce it).

That is where the conversation stands — memory recorded, lookups done, the code edits not yet made.

Standing constraints to preserve: Edit tool only for existing files (no sed/perl/awk edits; read-only grep/sed fine); commit/push only when asked (`/commitp` = `git add -A`, commit, push; tags stay local); don't point load generators at :8000 while Chris is working; ask before measurement runs; don't edit `~/.pi/agent/models.json` without asking; don't restart or change the MTPLX server or config without asking; posting to MTPLX #277 needs Chris's go-ahead; pi-dpm may diverge from opencode-dpm; address the user as Chris, use they/them; monitor long runs actively and report progress; ask before launching runs; don't edit extension/skill files while a run is live.
</analysis>

<summary>
1. **Primary Request and Intent:**

   **Background.** Repo `/Users/chris/Work/git/pi-dpm` ports opencode-dpm to pi. Chris tests dpm skills against a local MTPLX server (port 8000, 8-bit Quality build) through a scratchpad RPC driver (`drive.mjs`) that auto-answers gates.

   **Requests this segment, in order:**
   - (carried over) "Do the follow ups" — build the four findings from the mtplx-do1 dpm-do run.
   - `/commitp` — commit and push everything (`git add -A`, mandatory template, push to origin).
   - "Okay let's continue with our dpm testing" — resume live MTPLX testing.
   - (AskUserQuestion answers) "Next epic on the same project" / "Leave it as is" (MTPLX server stays as my background task).
   - "Record and build the fix" — record run 2's three findings to memory, and build the `coverage_claimed_at` server-stamping fix.

   **Standing constraints (still in effect):**
   - Use the Edit tool for existing files; no sed/perl/awk edits (read-only use of grep/sed for inspection is fine).
   - Commit or push only when asked (`/commitp` = commit and push everything with `git add -A`); tags stay local.
   - Don't point load generators at :8000 while Chris is working; ask before measurement runs.
   - Don't edit `~/.pi/agent/models.json` without asking.
   - Don't restart or change the MTPLX server or config without asking.
   - Posting to MTPLX #277 needs Chris's go-ahead.
   - pi-dpm may diverge from opencode-dpm.
   - Address the user as Chris; use they/them.
   - Monitor long runs actively and report progress (memory `monitor-long-runs`).
   - Ask before launching runs; don't edit extension/skill files while a run is live.

2. **Key Technical Concepts:**
   - **`SUPPLIED`** (`src/tools/convention.ts:134`): `{ ulid, clock, allocated, derived(from) }`. `serverSupplied` declares columns the server fills.
   - **Conformance** lives in `tests/support/conformance.js` (not src). Line ~110: "An argument may legitimately not be a column" — only arguments declaring an `enum` must name a column. So a boolean `verified`/`coverage_claimed` argument is legal.
   - **`bindings()`** (`tests/support/skills.js:775`, check at line 812): any argument a run passes to a `create_*`/`update_*` tool must appear in the SKILL.md inside a code span matching `` `<arg>\b ``. Because `\b` fails between `verified` and `_at`, `` `verified_at` `` does not satisfy a run passing `verified` — skill prose changes are machine-enforced.
   - **v0.7.0 parity freeze** (`tests/tool-naming.test.js`, oracle `tests/fixtures/v070-tool-surface.json`, 183 tools): the oracle may NEVER be regenerated. Deliberate drift is declared as a *pin*: `ADDED` (new tools), `REWORDED` (text at a path, compared with `assert.equal`), and now `RESHAPED` (renamed argument), applied to the oracle's copy by `withRewordings` before the byte-for-byte `deepEqual`.
   - **body-reads corpus** (`tests/support/body-reads.js` + `tests/body-reads.test.js`): every mention of a withholding tool in a skill is a "site" keyed `'<skill> · <tool> · <step>'` with `#2`, `#3` suffixes in document order; each needs a `[boolean, reason]` classification entry. Reasons quoting the file are checked with `entry.stepText.includes(phrase)` (step-scoped).
   - **Clock pinning idiom**: `spineTools(db, { now: () => X })`.
   - **dpm handoff**: per-story handoff opens a successor session; `dpm_adopt_session` carries continuity; context drops sharply (63.3k → 20.2k observed).
   - **MTPLX prompt-cache behaviour**: turns with `cacheRead: 0` and `input` ≈ 60k cost ~15 minutes; cache hits cost 16–98s.

3. **Files and Code Sections:**

   - **`src/tools/spine/coverage.ts`** (modified, committed in `2a69c39`) — follow-up 1.
     - Docblock rewritten: "neither half is the caller's. The caller says `verified: true`; the server's clock says when, and `src/coverage/binding.js` says over what… **Both halves are the server's because a model typed the other one.**"
     - `STATE`:
       ```ts
       const STATE = {
         position: { type: 'integer', minimum: 0, description: 'Display order only; not identity' },
         verified: {
           type: 'boolean',
           description: 'Record the verification, or clear it with false. The server stamps the time from '
             + 'its own clock and computes the binding hash that accompanies it',
         },
       };
       ```
     - `create_coverage`: `serverSupplied: { id: SUPPLIED.ulid, verified_at: SUPPLIED.clock, binding_hash: SUPPLIED.derived('the bound texts') }`; handler sets `verified_at: args.verified ? now() : null` and `binding_hash: args.verified ? bindingHash(db, args as Binding) : null`.
     - `update_coverage`: gained `serverSupplied: { verified_at: SUPPLIED.clock, binding_hash: SUPPLIED.derived('the bound texts') }`; handler:
       ```ts
       handler: ({ id, verified, ...changes }) => {
         if (verified === undefined) {
           return update(db, 'coverage', id, changes, 'update_coverage');
         }
         const binding = verified
           ? bindingHash(db, readById(db, 'coverage', id, 'update_coverage') as Binding)
           : null;
         return update(db, 'coverage', id, {
           ...changes, verified_at: verified ? now() : null, binding_hash: binding,
         }, 'update_coverage');
       },
       ```

   - **`skills/dpm-do/SKILL.md`** (modified, committed) — Step 5 now reads "…`dpm_update_coverage` with `verified: true`." plus "**Nothing here writes a table, clears a mark, computes a hash, or types a time**… An MTPLX run supplied its own `verified_at` — a plausible time it had never read off a clock…". **Line 314 still prescribes `dpm_update_requirement` and `coverage_claimed_at`** — this is the next edit needed.

   - **`tests/tool-naming.test.js`** (modified, committed) — new `RESHAPED` list after `REWORDED`:
     ```js
     const RESHAPED = [
       {
         tools: ['create_coverage', 'update_coverage'],
         from: 'verified_at',
         to: 'verified',
         now: {
           type: 'boolean',
           description: 'Record the verification, or clear it with false. The server stamps the time from '
             + 'its own clock and computes the binding hash that accompanies it',
         },
         why: 'the caller supplied the time of its own verification, and the server now stamps it',
       },
     ];
     ```
     `withRewordings` extended with `const renames = RESHAPED.filter(...)` and a loop doing `delete copy.inputSchema.properties[from]; copy.inputSchema.properties[to] = now;`. A validation loop asserts `entry.why`, `entry.from !== entry.to`, `Object.hasOwn(properties, entry.from)` and `Object.hasOwn(properties, entry.to) === false` against the oracle.

   - **`tests/support/body-reads.js`** (modified, committed) — two entries now:
     ```js
     'do · list_coverage · 8. Epic summary #2':
       [false, 'named only to be refused — *"Do not add up the `dpm_list_coverage` pages by hand"* — so the count is `check_coverage`\'s and no row is read here at all'],
     'do · list_coverage · 8. Epic summary #3':
       [false, 'the denominator is *"the bindings still standing"* — a count of the rows the call returns, and the fragment is judged by the read above it'],
     ```

   - **Ten test suites updated** (committed): `tools.test.js` (assertions to STAMP; new `verified_at` absent-from-schema check), `coverage-retirement-tool.test.js`, `substrate-amendments.test.js` (added `const AT = '2026-08-10T00:00:00.000Z'` and pinned its surface), `supersession-retirement.test.js`, `criterion-supersession.test.js`, `retired-claim.test.js`, `skill-do.test.js` (pinned `now` to '2026-08-09T00:00:00.000Z'; added `assert.ok(!passed.get('update_coverage').has('verified_at'), 'the run typed the time its own verification was made')`), `skills-corpus.test.js`, `skill-pivot.test.js` (two surfaces pinned to `VERIFIED`), `skill-ralph.test.js`, `skill-inspect.test.js`, `skill-status.test.js`, `coverage-check-tool.test.js` (surface pinned to AT; asserts `stamped.verified_at === AT`), `tests/support/self-hosting.js`, `src/preview/example.ts`.

   - **`src/tools/spine/requirement.ts`** (NOT yet modified — the target of the current task): `coverage_claimed_at:` in the schema at line 52; handler at line 126 is `handler: ({ id, coverage_claimed_at: claimedAt, ...changes }) => {`.

   - **`src/coverage/claim.ts`** (read via grep, not yet modified): lines 92–138 hold the claim logic — `UPDATE requirement SET coverage_claimed_at = ?, coverage_claim_hash = ? WHERE id = ?` and a reader returning `{ coverage_claimed_at, coverage_claim_hash }`. So `coverage_claimed_at` has a paired hash exactly like `binding_hash`.

   - **`/Users/chris/.claude/projects/-Users-chris-Work-git-pi-dpm/memory/dpm-do-run-followups.md`** (rewritten twice; current version records run 1's four findings as built+confirmed, run 2's three open findings, the v0.7.0/RESHAPED guidance, the `bindings()` `\b` rule, and the status.json-is-not-a-call-log lesson). MEMORY.md index line updated to: "— mtplx-do1's four findings, all built 2026-09-16; how to declare a tool-surface change against the v0.7.0 oracle".

   - **Run directory** `/private/tmp/claude-501/-Users-chris-Work-git-pi-dpm/5774d35e-880f-4bbc-8a79-499c991a65e7/scratchpad/mtplx-do1/`: `drive.mjs` (args: `project home out PROMPT`; runs pi with `--skill $REPO/skills -e $REPO/extensions/dpm`), `project/` (Go tally CLI + `.dpm/dpm.db`), `home/`, `out/`, `out2/`, `out3/` (this run).

4. **Errors and fixes:**
   - **Step 6 reword broke a test** (carried over): fixed by naming phases literally; verified green (27 tests).
   - **Three body-reads failures** — unclassified site `dpm-do · list_coverage · 8. Epic summary #3`, corpus 118 vs 117. Caused by the *earlier* follow-up 4 edit adding a third `dpm_list_coverage` mention to Step 8; that suite wasn't in my earlier targeted run. Fixed by rewriting `#2`'s reason for its real site and adding `#3`.
   - **Parity failure** `create_coverage differs from what v0.7.0 advertised` — `REWORDED` compares with `assert.equal` so it cannot express a rename. Fixed by adding the `RESHAPED` pin mechanism rather than regenerating the oracle (which `parity-v070.test.js` forbids).
   - **My wrong claim about `.claude/settings.local.json`** — I told Chris `/commitp` would sweep it up; it is matched by `**/.claude/settings.local.json` in `~/.gitignore`. Corrected explicitly.
   - **Three false suspicions during the run**, all from reading `status.json` as a call log when it carries only the last tool call of each tick: (a) "task 2 closed on an edit with no test run" — a `go test` ran at 6886s between the edit and the close; (b) "story 2 verified on stale evidence" — same evidence refutes it; (c) "`dpm_check_coverage` was never called" — it ran at 7768s sharing a timestamp with `dpm_list_taxonomy`. All three retracted explicitly; method corrected to read `events.jsonl` before naming a suspicion.

5. **Problem Solving:**
   - Follow-up 1 shipped without weakening the parity guarantee, by extending the declared-pin mechanism the file's own design calls for.
   - Full gate green before commit: **1284 tests, 0 failures**, `tsc --noEmit` clean, `npm run skills` and `npm run modules` clean.
   - Committed `2a69c39` on `main` (21 files, +518/−384) and pushed (`1edb65c..2a69c39`).
   - Live run 2 validated all four follow-ups; diagnosed the run's slowdown as prompt-cache misses rather than server degradation, and quantified the handoff's benefit (63.3k → 20.2k context).

6. **All user messages:**
   - "Okay let's continue with our dpm testing"
   - (AskUserQuestion answers) "Next epic on the same project" / "Leave it as is"
   - "Record and build the fix"
   - (`/commitp` slash-command invocation with its command body)

7. **Pending Tasks:**
   - **Build the `coverage_claimed_at` fix** (in progress): `update_requirement` takes `coverage_claimed: boolean`, declares `serverSupplied: { coverage_claimed_at: SUPPLIED.clock, coverage_claim_hash: ... }`, drops `coverage_claimed_at` from the caller schema; reword `skills/dpm-do/SKILL.md:314`; add a `RESHAPED` entry for `update_requirement` (`from: 'coverage_claimed_at'`, `to: 'coverage_claimed'`); update test call sites `tests/skill-do.test.js:286`, `tests/supersession-retirement.test.js:84,203`, `tests/retired-claim.test.js:84,130`; run the full suite, typecheck and sweeps.
   - Two other recorded findings not yet built: the second-story phase-label skip, and duplicate observations.
   - Nothing committed since `2a69c39`; commit only if Chris asks.

8. **Current Work:**
   Chris said "Record and build the fix". I completed the **record** half — rewrote `dpm-do-run-followups.md` with run 1's four findings (built and confirmed), run 2's three open findings, and the oracle/`bindings()`/status.json lessons. For the **build** half I gathered, in one parallel batch, everything the fix needs:
   - `grep -rn "coverage_claimed"` → `src/tools/spine/requirement.ts:52` (schema) and `:126` (`handler: ({ id, coverage_claimed_at: claimedAt, ...changes }) => {`); `src/coverage/claim.ts:92–138` (the `coverage_claimed_at` + `coverage_claim_hash` pair); `skills/dpm-do/SKILL.md:314`; tool call sites in `tests/skill-do.test.js:286`, `tests/supersession-retirement.test.js:84,203`, `tests/retired-claim.test.js:84,130`; direct-SQL sites in decay/integration/coverage-retirement-migration which are unaffected.
   - Oracle check: `update_requirement` props are `id, label, class, moscow, exclusion, parent_id, text, position, coverage_claimed_at`; its description is "ISO 8601. Claims the bound coverage rows account for this requirement whole; the server computes the hash over the bound set that accompanies it"; `has coverage_claimed: false` — so a `RESHAPED` pin is viable and the entry's `from`/`to` assertions will hold.

   No code edits for this fix have been made yet.

9. **Optional Next Step:**
   Read `src/tools/spine/requirement.ts` (around lines 40–140) — the one file I have not yet read in full and cannot edit without reading — then make the change: `coverage_claimed: boolean` with `serverSupplied` clock stamping and the paired `coverage_claim_hash`, mirroring `src/tools/spine/coverage.ts`; reword `skills/dpm-do/SKILL.md:314`; add the `update_requirement` entry to `RESHAPED`; update the four test call sites; then run `npm test`, `npm run typecheck`, `npm run skills`, `npm run modules`.

   This follows directly from Chris's "Record and build the fix" and from my stated plan for it: "**`coverage_claimed_at` is still caller-supplied.** The model shelled out to `date -u` for it — good instinct, nothing requires it. That's the defect we just removed from `verified_at`, alive in the column beside it. The fix is the same shape and would be small." Do not commit unless Chris asks.
</summary>
