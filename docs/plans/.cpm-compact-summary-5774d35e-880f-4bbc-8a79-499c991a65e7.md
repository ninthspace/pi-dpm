# Compact Summary (PostCompact)
**Captured**: 2026-09-15 14:16:30
**Trigger**: auto

<analysis>
**Where this segment started.** The previous summary covered "Fix things first": the `requirement_label` derived field, the `UNRENDERED` wording, and edits to the skill and shared conventions. In this segment:

**Fixes finished and epics6 run.**
- Added a `requirement_label` test to `tests/coverage-check-tool.test.js`. Suite 1269 green, tsc clean, skills check clean.
- Found that pi replays reasoning as `reasoning_content`.
- Chris chose: 8-bit build, `--preserve-thinking auto`, 5G bank, fresh epics run.
- Restarted MTPLX and set up epics6. Chris said "Stop" and "I'll tell you when to start"; I stopped the driver and monitor. Chris then said "Okay. You can start now".
- Launched epics6 with the prompt "/dpm-epics".
- Chris: "Please monitor so that I can be informed of progress. Remember this". I saved the monitor-long-runs memory and set up a Monitor.
  - The first monitor's `pgrep` matched its own command; I fixed it with a `[n]ode` bracket pattern.
- Progress reported:
  - gates 1–9 done;
  - epic 1 (csv-parsing) fully done with 10 bindings;
  - a 10-minute long turn;
  - retokenized_history_mismatch cache saves (10 of 18 stored).

**Per-epic restart idea.**
- Chris asked whether it's useful to stop after each epic and restart from empty context. I recommended a handoff design.
- Chris: "Agreed. The thing I like about this is that it's independent of model and number of epics". I saved the epics-per-epic-handoff memory.
- epics6 OOMed at 86k prompt tokens (6708s). My monitor missed the error; I updated the memory with how to detect it.

**Handoff built.**
- Chris: "Build the handoff. Should we build similar for other skills?"
- Research findings:
  - pi's `newSession` exists only on command contexts.
  - `sendUserMessage` with `expandPromptTemplates` dispatches extension commands.
  - `withSession` gives a fresh ctx with `sendMessage` and `sendUserMessage`.
  - `resourceLoader.reload` runs per runtime, so extensions reload on a new session.
- Built `extensions/dpm/handoff.ts`, the index wiring, and the test updates. Full suite 1274 green.
- Also built the shared Handoff procedure, epics skill edits, the epics7 setup and a new driver.
- Recommendations for other skills: dpm-do next (per story); dpm-ralph probably (it lacks the stop hook); measure dpm-spec first; the rest don't need it.

**Epics7.**
- Chris: "Start on current server". Launched with "/dpm-epics 01".
- Handoff 1 at 3043s worked: fresh session prompt 19.6k, adopt_session first, re-read without re-gating.
- Handoff 2 at 6134s: the model called handoff again (6265s) and never ended the turn. It hallucinated "Continued run: adopted…" and continued epic 3 in the old context, writing 2 stories and a dependency without a gate.
- I asked Chris; answer: "Stop, fix, resume (Recommended)".
- Stopped the driver and monitor; no pi processes left.
- Backed up the DB to `mtplx-epics7/dpm-before-resume.db`; deleted epic 3's 2 stories and 1 dependency.
- Fixed `handoff.ts`: a `tool_call` block after acceptance, and abort on `turn_end`. Updated the test to expect 2 requests.
- Handoff tests 5/5 pass, tsc clean, full suite 1274 pass.

**Next.** Relaunch the epics7 driver as a resume and monitor it, which Chris approved as part of "Stop, fix, resume".
- The out dir has the old gates.md and events. Move them aside (out-run1) and create a new out directory.
- Note: without a handoff note, the resumed run in a fresh pi process relies on Session Startup listing the open session (`01a0a4ea-7965-7684-bdd3-70d523ccbd38`, phase stories) and adopting it.

Also other details: the "Also shared Handoff section" was added, and the epics SKILL edits.
</analysis>

<summary>
1. Primary Request and Intent:
   **Background.** Repo `/Users/chris/Work/git/pi-dpm` ports opencode-dpm to pi. Chris tests dpm skills, mainly dpm-epics, on a local MTPLX server (port 8000), driven by a scratchpad RPC driver script that auto-answers gates.

   **Sequence of requests this segment:**
   - **"Fix things first"** (from before): finish `requirement_label`, the render-gate wording, and the thinking/cache fix. Then choose server settings. Chris picked "8-bit, thinking auto, 5G bank" and "Fresh epics run".
   - **"Stop"** and **"I'll tell you when to start"**, then **"Okay. You can start now"**.
   - **"Please monitor so that I can be informed of progress. Remember this"**: actively monitor long runs and report progress. Saved to memory.
   - **"Just a thought: for the epics skill, is it useful to stop after each epic and restart from an empty context?"** Then **"Agreed. The thing I like about this is that it's independent of model and number of epics"**.
   - **"Build the handoff. Should we build similar for other skills?"**
   - **"Start on current server"**: epics7 on the unchanged 8-bit server.
   - **Latest decision** (AskUserQuestion answer): **"Stop, fix, resume (Recommended)"**. That is: stop epics7, make an accepted handoff block further tool calls and force the turn to end (with a test), then restart the driver with `/dpm-epics 01` in a fresh process, which resumes from the rows at epic 3.

   **Standing constraints (still in effect):**
   - Use the Edit tool for existing files; no sed/perl/awk edits.
   - Commit or push only when asked; tags stay local.
   - Don't point load generators at `:8000` while Chris is working; ask before each measurement run.
   - Don't edit Chris's `~/.pi/agent/models.json` without asking.
   - Don't restart or change Chris's MTPLX server or config without asking. Chris authorised running and stopping MTPLX here; check before each restart that changes a setting.
   - Posting to MTPLX #277 requires Chris's go-ahead.
   - pi-dpm may diverge from opencode-dpm freely.
   - Address the user as Chris; use they/them.
   - Monitor long runs actively and report progress (memory `monitor-long-runs`).
   - Ask before launching runs. The current resume relaunch is already approved via "Stop, fix, resume".
   - Don't edit extension files while a run is live: each new session reloads the extension.

2. Key Technical Concepts:

   **Hardware and server.**
   - M1 Max, 64 GB. MTPLX engine budget is 48 GB.
   - Current server: 8-bit Quality build, served id `mtplx-qwen38-27b-optimized-quality-fp16`, 27.9 GB of weights. Still running and idle. Log: `scratchpad/mtplx-server/serve-quality2.log`. Command:
     ```
     MTPLX_SESSION_BANK_MAX_BYTES=5G MTPLX_SESSION_BANK_PER_SESSION_BYTES=5G exec mtplx serve --port 8000 --no-auth --fan-mode smart --enable-thermal-poll --preserve-thinking auto --tool-prompt-mode native --model Youssofal/Qwen3.8-27B-MTPLX-Optimized-Quality-FP16
     ```
   - pi replays reasoning as `reasoning_content` (the `thinkingSignature`). With preserve-thinking auto, most turn stores work. Some still fail with `retokenized_history_mismatch`.

   **Memory findings.**
   - The OOM happens during prefill at around 86k prompt tokens on the 8-bit build, even with the bank trimmed to 0.
   - The error shows in pi events as `stopReason: "error"`, `errorMessage: "insufficient memory…"`, and in the server log as `pressure_trim` / `allocation_failure_shed`.
   - Flight metrics: `~/.mtplx/metrics/flight-8000.jsonl`. Keys are `"ev": "begin"|"end"|"s"|"pc"`, with a space after the colon.

   **pi extension API facts.**
   - `ExtensionCommandContext` has `newSession({parentSession, setup, withSession})`, `waitForIdle`, and more. Tool `execute` and event handlers only get `ExtensionContext`, which has `abort()` and `sessionManager.getSessionId()`/`getSessionFile()` but no `newSession`.
   - `pi.sendUserMessage(text, {expandPromptTemplates: true})` goes through `prompt()`, which runs `_tryExecuteExtensionCommand` for `/name`.
   - `withSession(fresh)` gets `ReplacedSessionContext`, which has `sendMessage({customType, content, display}, {deliverAs: 'nextTurn'})` and `sendUserMessage`.
   - `newSession` calls `teardownCurrent` (abort plus `session_shutdown`), then `createRuntime`, which reloads extensions and emits `session_start` with reason "new".
   - `agent_settled`: the extension handler runs first, then the RPC emit.
   - RPC mode rebinds its event subscription on session replacement, so the driver keeps receiving events.

   **Handoff design.**
   - The skill calls the `handoff` tool, which records a pending `/dpm-continue <skill> <args>`.
   - Once accepted: every later `tool_call` is blocked, and `turn_end` calls `ctx.abort()`.
   - On `agent_settled`, `setTimeout` sends the command.
   - The command runs `newSession`; `withSession` sends the hidden continuation note (customType `dpm-handoff`) as nextTurn, then `/skill:<skill> <args>`.
   - The continued run adopts the predecessor via `dpm_adopt_session` (`id`, `predecessor_id`, `include_body`).

   **Test infrastructure.**
   - The skills check's `HOST_MECHANISM` list doesn't flag `handoff`.
   - `CALLABLE` is `dpm_`, so `handoff` in skill bodies isn't treated as a registry tool.
   - The allowlist derives only `dpm_*` names, so unprefixed tools like `question` and `handoff` stay active for every skill.

3. Files and Code Sections:

   - **`extensions/dpm/handoff.ts`** (NEW, then fixed). Final key code:
     ```ts
     import type { ExtensionAPI, ToolDefinition } from '@earendil-works/pi-coding-agent';
     import { skillInvoked } from './activation.ts';
     import { PREFIX } from './adapter.ts';
     import { skillMessage } from './commands.ts';

     export const HANDOFF = 'handoff';
     export const CONTINUE = 'dpm-continue';
     export const NOTE = 'dpm-handoff';
     export type Invocation = { readonly skill: string; readonly args: string };
     export function invocation(text: string): Invocation | null { const skill = skillInvoked(text); if (skill === null) return null; return { skill, args: text.slice(`/skill:${skill}`.length).trim() }; }
     export function continued(args: string): Invocation | null { /* first word = skill, rest trimmed = args; '' → null */ }
     export const continueCommand = ({ skill, args }: Invocation): string => (args === '' ? `/${CONTINUE} ${skill}` : `/${CONTINUE} ${skill} ${args}`);
     export const continuation = (predecessor: string): string =>
       `This run continues a dpm skill run handed off from the harness session ${predecessor}, so Session `
       + `Startup is a resume: call ${PREFIX}adopt_session with this run's session id as \`id\` and `
       + `${predecessor} as \`predecessor_id\`, passing \`include_body\`, and carry on from the phase and state it `
       + 'returns. What the earlier run settled is in that state and in the rows, and is not facilitated again.';
     export const handedOff = (skill: string): string =>
       `${HANDOFF}: accepted. This turn ends here — ${skill} continues in a fresh context, and that run `
       + 'adopts this session.';
     export const ALREADY_HANDED_OFF = `${HANDOFF}: this run has already handed off, so nothing more is done in `
       + 'this context. The run continues in a fresh one once this turn ends.';
     export const NOTHING_RUNNING = `${HANDOFF}: no dpm skill was invoked in this session, so there is no run to `
       + 'continue. Nothing was handed off; carry on in this context.';

     export function registerHandoff(pi: ExtensionAPI, skills: ReadonlySet<string>): void {
       let running: Invocation | null = null;
       let pending: string | null = null;
       pi.on('session_start', () => { running = null; pending = null; });
       pi.on('input', (event) => { const invoked = invocation(event.text); if (invoked !== null && skills.has(invoked.skill)) running = invoked; });
       pi.registerTool({ name: HANDOFF, label: 'Handoff', description: 'End this skill run here and continue it in a fresh context, where it adopts this session. Record everything the rest of the run needs in the session state first: the fresh context has only that state and the rows.',
         parameters: { type: 'object', additionalProperties: false, properties: {} } as unknown as ToolDefinition['parameters'],
         executionMode: 'sequential',
         async execute() { if (running === null) throw new Error(NOTHING_RUNNING); pending = continueCommand(running); return { content: [{ type: 'text', text: handedOff(running.skill) }], details: { continues: pending } }; } });
       pi.on('tool_call', () => (pending === null ? undefined : { block: true, reason: ALREADY_HANDED_OFF }));
       pi.on('turn_end', (_event, ctx) => { if (pending !== null) ctx.abort(); });
       pi.on('agent_settled', () => { if (pending === null) return; const command = pending; pending = null;
         setTimeout(() => pi.sendUserMessage(command, { expandPromptTemplates: true }), 0); });
       pi.registerCommand(CONTINUE, { description: `Continue a dpm skill run in a fresh context that adopts this session: /${CONTINUE} <skill> [arguments]`,
         handler: async (args, ctx) => {
           const target = continued(args);
           if (target === null || !skills.has(target.skill)) throw new Error(`dpm: /${CONTINUE} continues a dpm skill, as /${CONTINUE} dpm-epics 01 — "${args.trim()}" names none`);
           await ctx.waitForIdle();
           const predecessor = ctx.sessionManager.getSessionId();
           await ctx.newSession({ withSession: async (fresh) => {
             await fresh.sendMessage({ customType: NOTE, content: continuation(predecessor), display: false }, { deliverAs: 'nextTurn' });
             fresh.sendUserMessage(skillMessage(target.skill, target.args), { expandPromptTemplates: true })
               .catch((error: unknown) => fresh.ui.notify(`dpm: /${CONTINUE} could not start ${target.skill}: ${error instanceof Error ? error.message : String(error)}`, 'error'));
           } });
         } });
     }
     ```
     The docblock explains:
     - epics6 OOM numbers;
     - why a tool plus a command;
     - the continuation note;
     - that the skill decides where to hand off;
     - the epics7 failure: a second handoff call, a hallucinated "Continued run", stories written without a gate. Hence the block plus abort.

   - **`extensions/dpm/index.ts`**
     - Docblock line added: "- `handoff.ts` — `handoff` and `/dpm-continue`, a run continued in a fresh context."
     - Added `import { registerHandoff } from './handoff.ts';`.
     - Added `registerHandoff(pi, new Set(skills.map((skill) => skill.name)));` after `activateSkills`.

   - **`tests/pi-handoff.test.js`** (NEW; registered in suite-integrity `ADDED` as `'pi-handoff.test.js', // an MTPLX epics run — a skill run continued in a fresh context that adopts the old session`, between pi-gate and pi-phases). Five tests:
     1. Invocation and continue-command parsing round-trips.
     2. The continuation note names `dpm_adopt_session`, `id`, "pi-session-1 as `predecessor_id`" and `include_body`.
     3. **Integration (updated):**
        - script `[callsTool(HANDOFF, {}, SAID), answers('Continued.')]`, run `runPrompt(scratch, '/skill:dpm-epics 01', { args: ['--skill', SKILLS], settles: 2 })`;
        - asserts `model.requests.length === 2` and no tool results in the new session's messages;
        - the resumed request's user text includes `<skill name="dpm-epics"`, matches `/<\/skill>\n\n01/`, and includes `continuation(handedFrom)`;
        - a different announced id; `state.sessionId` matches; the announcement is said once;
        - SAID is not present, and there are no tool messages.
     4. A handoff with no skill running: isError, text equals `NOTHING_RUNNING`, 2 requests.
     5. `/dpm-continue not-a-skill 01` produces one extension error matching `/"not-a-skill 01" names none/` and 0 requests.

     Helpers: `userText`, `allText`, `announcedId` (regex `/The harness session id for this run is (\S+)\. Pass it/`).

   - **`tests/support/pi.js`**: `runPrompt` gained a `settles = 1` option. It counts `agent_settled` and calls `finish` when `settled >= settles`. The docblock explains that a handoff settles once per session.

   - **`tests/pi-extension.test.js`**: imports `HANDOFF`; expected tools are `[...expected, GATE, HANDOFF].sort()`, and the comment says "the gate and the handoff".

   - **`tests/pi-skills.test.js`**: imports `HANDOFF`; `expected = [...derived['dpm-spec'], GATE, HANDOFF].sort()`; the control test expects `[...registered, GATE, HANDOFF].sort()`.

   - **`tests/coverage-check-tool.test.js`**: added the test "every coverage row names its requirement by label, whichever tool returns it". It checks `requirement_label` is 'FR1' from create, read, list, update and retire coverage.

   - **`shared/skill-conventions.md`**: new `## Handoff` section between Session Startup and Library Check.
     - The purpose: one context holds one unit.
     - Steps:
       1. Put in `state` everything not a row (the next unit, and anything promised to a later unit).
       2. `dpm_update_session` with state and the phase to start at.
       3. One line saying what's finished and what continues.
       4. Call the host's `handoff` tool, then end the turn. With no tool, tell the user to start a fresh session and invoke the skill with the same args.
     - "A continued run is a resume, and it is told so": adopt; don't re-facilitate what is settled; re-read what the next step works from.
     - Earlier additions (Gate Presentation, "Every gate renders its own draft…") remain.

   - **`skills/dpm-epics/SKILL.md`**
     - **Startup › Session** addition: "**So does anything settled for a later epic, as it is settled.**" A binding left to another epic (e.g. FR3's exit-code half) goes into `state` naming the requirement, the clause and the epic; that epic's Step 3d binds it. Otherwise it's lost at handoff and Step 4 won't find it.
     - **Process** additions:
       - "**Each epic ends with a handoff**, following the shared **Handoff** procedure": after the epic's bindings are read back, record it done plus the next epic, and hand off; hand off once more after the last epic, so Step 4 starts fresh.
       - "**A continued run starts at the epic its `state` names.**" It re-reads Step 1's source (same bound and bodies), lists the epics, doesn't gate Steps 1–2 again or re-present library/decisions/retros, then follows the resume order under **Session**.
     - Earlier edits (`requirement_label` read-back sentence, Process render paragraph, Step 4 `check_coverage`) remain.

   - **Earlier in the segment** (from the previous summary, untested then and now tested/green): `src/tools/spine/coverage.ts` `withRequirementLabel` derived field; `extensions/dpm/gate.ts` extended `UNRENDERED`.

   - **Memory files** (`/Users/chris/.claude/projects/-Users-chris-Work-git-pi-dpm/memory/`):
     - `monitor-long-runs.md` (feedback): monitor long runs actively and report progress. Includes how to catch OOM (events `stopReason: "error"`; server `pressure_trim`/`allocation_failure_shed`) and the `[n]ode` pgrep trick.
     - `epics-per-epic-handoff.md` (project): the plan, why, and how to apply. A status line says it's built and uncommitted; next steps are epics7, then consider dpm-do and dpm-ralph.
     - `MEMORY.md` index updated with both.

   - **Scratchpad** (`S=/private/tmp/claude-501/-Users-chris-Work-git-pi-dpm/5774d35e-880f-4bbc-8a79-499c991a65e7/scratchpad`):
     - **`mtplx-epics6/`**: out-aborted plus out. OOM at 6708s after epic 1 (csv-parsing: 3 stories, 10 bindings). Gates: 9.
     - **`mtplx-epics7/`**: project, home (8-bit model id), `monitor.sh`, `drive.mjs` (new), `out/` (current run logs), `dpm-before-resume.db` (backup before deleting epic 3 stories).
       - `drive.mjs` is epics6's driver plus:
         - follows handoffs: a `tool_execution_end` for `handoff` that isn't an error sets `handedOff`; the next `agent_settled` logs "settled after handoff N; continuing in a fresh session" instead of finishing;
         - "## Handoff N" headings;
         - `[MODEL_ERROR]` logging and a `modelErrors` count from `message_end` `stopReason === 'error'`;
         - notify notices;
         - status includes `modelErrors` and `handoffs`;
         - `LIMIT_MS` 360 min; gate cap 120.
       - Launched as `cd $S/mtplx-epics7/project && node $S/mtplx-epics7/drive.mjs $S/mtplx-epics7/project $S/mtplx-epics7/home $S/mtplx-epics7/out "/dpm-epics 01"`.
       - **epics7 DB state after cleanup:**
         - cli-contract: 2 stories, 10 criteria, 6 tasks, 13 bindings;
         - csv-ingest: 2 stories, 14 criteria, 4 tasks, 14 bindings;
         - aggregation-and-output: 0 stories;
         - repo-and-env-baseline: 0 stories.
       - **epics7 sessions:** `01a0a4b9-…` superseded by `01a0a4ea-7965-7684-bdd3-70d523ccbd38`, which has phase `stories`. Its state holds `next_epic` "3 aggregation-and-output" (or epic-2 done), `epics_done` and `deferred_bindings`.
     - **Monitor command pattern** (used for epics7):
       - waits for gates.md and the driver;
       - tails gates.md for `^## Gate|^## Handoff|TOOL_ERROR|MODEL_ERROR|phase|notice|extension_error|refused|settled after|^answered:|^finished:`;
       - watches the server log for `insufficient memory|out of memory|pressure_trim|allocation_failure|traceback|fatal`;
       - prints STATUS every 120 ticks (5 s sleep);
       - prints RUN ENDED when `pgrep -f "[n]ode .*mtplx-epics7/drive.mjs"` fails;
       - reports the health check failing after 3 misses.

4. Errors and fixes:
   - **Monitor pgrep matched its own command**, so it would never report the run ending. Fixed with the `[n]ode .*mtplx-epicsN/drive.mjs` pattern.
   - **epics6 OOM not caught by the monitor.** The error was only in pi events and `pressure_trim` lines, and the monitor grepped the server log for "insufficient memory". Fixed the patterns for epics7 and recorded it in memory.
   - **epics6 OOM at 86k** even with the 5G bank and preserve-thinking auto: 8-bit weights plus long prefill exceed 48 GB. Solution: the per-epic handoff, which Chris agreed to.
   - **Edit to `shared/skill-conventions.md` failed** (string not found: the bold markers differed). Re-read lines 44–50 and anchored on "goes stale the moment the row moves.\n\n## Library Check".
   - **zsh `--include=*.js` "no matches found"**: avoided that flag.
   - **epics7 second handoff failure.** The model called `handoff` twice and didn't end its turn. It hallucinated a continuation and wrote epic 3's stories and a dependency without a gate in the old 63k context. Fixes:
     - stopped the run with Chris's approval;
     - backed up the DB and removed epic 3's two stories and one dependency;
     - `handoff.ts` now blocks all tool calls after acceptance (`ALREADY_HANDED_OFF`) and aborts on `turn_end`;
     - the test now expects no old-context request (2 requests total).
   - **Epics7 blocked gates:** 2, both recovered via the guard. **Refused coverage write:** the FR9 fragment was sent under FR10's id; the model correctly rebound it to FR9 per the refusal.

5. Problem Solving:
   - **Handoff 1 in epics7 worked end to end:**
     - the state recorded `next_epic`, `epics_done` and `deferred_bindings`;
     - the old closing turn was 63.8k, the fresh session's first request 19.6k;
     - the note named the correct predecessor; `dpm_adopt_session` was called first;
     - it re-read the spec, requirements and sections without re-gating, then `dpm_list_story` for epic 2, and gated stories.
   - **Handoff 2 failure was diagnosed and fixed** as above. Tests: pi-handoff 5/5, full suite 1274/1274, tsc clean.
   - **Observations for end-of-run review:**
     - some FR3 bindings quote the requirement's whole opening sentence rather than the narrowest clause;
     - the session phase label for integration/coverage is still skipped;
     - `retokenized_history_mismatch` cache stores (an MTPLX issue).
   - **Other skills recommendation** (already reported to Chris): dpm-do next (per story); dpm-ralph probably (the handoff could replace its missing stop hook); dpm-spec measure first; the rest no.

6. All user messages:
   - "Stop"
   - "I'll tell you when to start"
   - "Okay. You can start now"
   - "Please monitor so that I can be informed of progress. Remember this"
   - "Just a thought: for the epics skill, is it useful to stop after each epic and restart from an empty context?"
   - "Agreed. The thing I like about this is that it's independent of model and number of epics"
   - "Build the handoff. Should we build similar for other skills?"
   - "Start on current server"
   - AskUserQuestion answers:
     - server: "8-bit, thinking auto, 5G bank (Recommended)"; run: "Fresh epics run (Recommended)" (earlier in the segment);
     - "Epics7 is carrying on with epic 3 in the old context…": "Stop, fix, resume (Recommended)".
   - Also, from before: "Fix things first".

7. Pending Tasks:
   - **Resume epics7:** restart the driver with `/dpm-epics 01` in a fresh process, which resumes from the rows at epic 3 (approved). Monitor it and report progress, as memory requires.
   - Review at end of run: gap check, final tree, deferred bindings honoured, handoff count, OOM or not, blocked gates, broad fragments.
   - **Later (not started; ask first):** apply the handoff to dpm-do (per story) and possibly dpm-ralph; measure dpm-spec.
   - Nothing is committed. Commit only if Chris asks.

8. Current Work:
   Executing Chris's "Stop, fix, resume (Recommended)". Done so far:
   - stopped the epics7 driver (`bkgjgh6j0`) and monitor (`bym6cuj9q`); no pi processes remain; the server is idle;
   - backed up the DB to `mtplx-epics7/dpm-before-resume.db` and deleted aggregation-and-output's 2 ungated stories and 1 dependency (0 stories remain);
   - fixed `extensions/dpm/handoff.ts` (tool_call block after acceptance, plus `turn_end` abort, new `handedOff` text and `ALREADY_HANDED_OFF`);
   - updated `tests/pi-handoff.test.js`.

   Verification just finished: pi-handoff tests 5/5 pass, `tsc --noEmit` clean, `npm test` 1274/1274 pass. What remains is relaunching epics7 and monitoring.

9. Optional Next Step:
   Relaunch epics7 as a resume and monitor it, per Chris's choice "Stop, fix, resume (Recommended) — … Then restart the driver with /dpm-epics 01 in a fresh process, which resumes from the rows at epic 3."
   1. Move the existing `mtplx-epics7/out` aside (e.g. to `out-run1`) and create a new `out`.
   2. Launch in the background: `cd $S/mtplx-epics7/project && node $S/mtplx-epics7/drive.mjs $S/mtplx-epics7/project $S/mtplx-epics7/home $S/mtplx-epics7/out "/dpm-epics 01"`.
   3. Start a persistent Monitor with the epics7 patterns: gates, handoffs, TOOL_ERROR, MODEL_ERROR, phase, notices, settled after, server pressure, STATUS, RUN ENDED.
   4. Check early that Session Startup adopts the open session `01a0a4ea-7965-7684-bdd3-70d523ccbd38` (phase stories) and resumes at epic 3 with a stories gate. Then report progress to Chris.
</summary>
